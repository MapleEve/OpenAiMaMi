# Maintenance Cluster — AiMaMi 1.2.3 macOS arm64 — Distilled

Domain per task instruction: 注册表 rebuild/clean/load_snapshot 域 (registry rebuild/clean/load_snapshot). Same 3-leaf scope as the existing `macos-1.0.9-maintenance` package. This is a read-only distillation pass over already-landed raw evidence — no new IDA decompilation performed.

## Leaf: clean

- **Frontend (dim1): PASS.** `raw/aimami/1.2.3/macos-arm64/frontend/pages/analytics-maintenance-autoswitch-frontend.md` §2.3 documents a real MaintenancePage action card (item 4 of 7 in the `oe` config array, 569-636 行): `onAction: () => n("clean", () => U.mutateAsync())` → invoke `clean`. `U` is a `useMutation` whose `onSuccess` runs a full `invalidateQueries()` (no queryKey filter — cache-wide invalidation, which also causes the §1 AnalyticsPanel queries to refetch on next visit) and shows a toast echoing `cleanResult` with fields `authBackupsRemoved` / `registryBackupsRemoved` / `staleEntriesRemoved`. `frontend-control-flow.jsonl` independently confirms the terminal invoke call `re("clean")`, no args.
- **Backend owner (dim2): PASS.** `commands/system/ida/pseudocode/clean_0x1007cd710.c` (139 lines, header `基线 same-set`) — full non-stub body: acquires a `OnceBox`-backed mutex (handles poisoned-lock path explicitly), calls `codexmate_lib::core::repository::Repository::clean::h249caf1a46522ec0(&v19, a2 + 2)`, maps the result through `CoreError::fmt` on the error branch, returns a `_QWORD*` result envelope.
- **Backend core (dim2): PASS.** `core/repository/ida/pseudocode/clean_0x100a74760.c` (618 lines, header `基线 same-set`) — real, substantial body (not walked line-by-line beyond the first 40 lines this pass, but confirmed non-stub: no truncation marker, real local-variable layout typical of a full Hex-Rays decompile).
- **Call-tree (dim3): PARTIAL.** Immediate core owner confirmed. Candidate downstream leaves (by naming pattern + 1.0.9 documented shape): `load_registry_0x100a56040.c` (49 lines) → `persist_registry_0x100a5b440.c` (338 lines) as the likely terminal persistence write, matching 1.0.9's documented `clean → load_registry → ... → persist_registry → quota_store` shape. NOT independently re-traced via callees()/xrefs_to() this pass.
- **Interface (dim4): PARTIAL.** No params (matches 1.0.9 and ipc-contracts.jsonl `argKeys: []`). Response field names known only from the frontend toast-echo narrative: `authBackupsRemoved`, `registryBackupsRemoved`, `staleEntriesRemoved`. This differs in naming from the 1.0.9 package's byte-confirmed `CleanResult{deleted_sessions, deleted_intermediate, registry_delta}` — see DELTA-LOGIC-1.2.3.md. Not independently byte-confirmed against the 1.2.3 backend struct this pass.
- **Errors:** Mutex poisoned path visible in the wrapper (explicit `"poisoned lock: another task failed inside"` branch). Deeper error paths (load_registry fail / persist_registry fail) not re-verified this pass, carried as a plausible-by-analogy note from 1.0.9 only.
- **Platform (dim5):** macOS confirmed only. Windows sibling (`windows-1.2.3-maintenance`) exists independently — not consumed here.
- **Acceptance (dim6):** missing (C5 implementation side).
- **Gate leaf:** consumerStartReady (startMode=reverse_guided).

## Leaf: rebuild_registry

- **Frontend (dim1): GAP.** `frontend-control-flow.jsonl` has exactly one entry for `rebuild_registry`, and it is `"trigger":{"type":"minified-static-function","element":null}` — i.e. the automated extractor found the `rebuildRegistry: ()=>re("rebuild_registry")` wrapper *definition* inside the shared service facade object, but resolved no calling element. `rebuild_registry` does **not** appear in MaintenancePage's 7-item action-card list (`analytics-maintenance-autoswitch-frontend.md` §2.3), and a grep of `accounts-frontend.md` and `shared-index-app-frontend.md` for `rebuildRegistry`/`rebuild_registry` returned zero hits. No page-narrative doc in the currently-landed 1.2.3 raw traces any UI element to this command. This is a genuine open gap in the raw evidence, not an examined-and-accepted unknown.
- **Backend owner (dim2): PASS — strongest of the 3 leaves.** `commands/system/ida/pseudocode/rebuild_registry_0x1007c7fb0.c` (143 lines, header `基线 same-set`) — same mutex-lock/CoreError-handling shape as `clean`, calls `codexmate_lib::core::repository::Repository::rebuild_registry_with_policy::hbfa144768b254519(v20, a2 + 2, 0, 0)` — note the two extra literal-`0` arguments beyond the `clean`/`load_snapshot` wrappers' call shape.
- **Backend core (dim2): PASS, with a confirmed version-delta annotation.** `core/repository/ida/pseudocode/rebuild_registry_with_policy_0x100a6a890.c` (2132 lines) carries the header comment `// 1.2.3 NEW-delta | codexmate_lib::core::repository | vs 1.2.2 新增(IDA函数枚举diff, 名↔地址一致)` — i.e. a prior producer pass's IDA function-enumeration diff already flagged this exact function as new/changed since 1.2.2. Only the first ~90 lines (function signature + local-variable declarations) were read this pass; the remaining ~2040 lines were not walked line-by-line (out of scope for a read-only distillation pass — see manifest.json known_residuals).
- **Call-tree (dim3): PARTIAL.** Candidate downstream leaves by 1.0.9 shape analogy: `persist_registry_0x100a5b440.c` as likely terminal (1.0.9: `rebuild → load_auth_file → make_auth_snapshot → persist_registry`). Not independently re-traced.
- **Interface (dim4): MISSING.** No 1.2.3 frontend narrative confirms request or response fields for this command (it isn't in any read UI-action doc). The wrapper takes no request args (`argKeys: []` per ipc-contracts.jsonl). Response shape unknown at 1.2.3; 1.0.9 had `RebuildRegistryPayload{activeAccountKey, accountCount, registryUpdated}` but this is NOT assumed to carry forward given the confirmed core-function signature change.
- **Errors:** Same poisoned-mutex path pattern as `clean`/`load_snapshot` visible in the wrapper. Core-level error paths (ensure_directories/load_auth_file/persist_registry per 1.0.9 shape) not re-verified.
- **Platform (dim5):** macOS confirmed only. The windows-1.2.3-maintenance sibling independently found the identical "policy parameter added, semantics undetermined" signal for its own `rebuild_registry_with_policy` evidence — noted here only as a same-day independent cross-check of the diff signal's existence, not used to fill any macOS gap.
- **Acceptance (dim6):** missing.
- **Gate leaf: consumerStartBlocked.** Backend evidence is the strongest of the 3 leaves, but `uiCallEntryReady` is not satisfied — per GATE-SPEC.md Gate 1 this blocks consumerStartReady regardless of backend strength.

## Leaf: load_snapshot

- **Frontend (dim1): PARTIAL.** Two `frontend-control-flow.jsonl` entries: (1) the bare wrapper-definition site (`element:null`, same weak signal as clean/rebuild_registry), and (2) a real guarded wrapper-callsite: `Ee.loadSnapshot(!1,x)` called from a function `c` that has an explicit preflight guard `if(!x||C.forceMetadata)return C.promise;` (single-flight/dedup pattern) — this is stronger automated evidence than either `clean` or `rebuild_registry` get. However, the outermost UI element that ultimately triggers function `c` was not resolved by the automated extractor, and `accounts-frontend.md`'s only related passage explicitly labels its connection as an inference (`**推断**`): a "刷新" (refresh) icon button's `onClick` calls a prop `refreshLive({forceMetadata})`, and the prop's parameter shape happens to match `load_snapshot`'s `(localOnly, forceMetadata)` signature, but `refreshLive` itself is not defined in the read file — the author explicitly says the correspondence needs parent-component (Dashboard/App-level) cross-verification not performed this pass.
- **Backend owner (dim2): PASS.** `commands/accounts/ida/pseudocode/load_snapshot_0x1006ffc90.c` (146 lines, header `基线 same-set`) — a doubly-nested async closure (`load_snapshot::{{closure}}::{{closure}}`) that acquires the same `OnceBox`-mutex pattern, then calls `codexmate_lib::core::repository::Repository::load_snapshot_local::h7b714ce076bcba03(__src)`. Module placement note: this wrapper lives under `commands::accounts`, not `commands::system`, despite being in-scope for this registry/maintenance domain — same placement noted in the 1.0.9 package.
- **Backend core (dim2): PASS.** `core/repository/ida/pseudocode/load_snapshot_local_0x100a5c1d0.c` (96 lines, header `基线 same-set`) — confirmed real non-stub body.
- **Notable evidence gap vs 1.0.9 characterization:** the 1.2.3 closure body calls `Repository::load_snapshot_local(__src)` with no visible `localOnly`/`forceMetadata` arguments beyond the destination buffer — differing from the 1.0.9 package's description of `load_snapshot` as accepting a `localOnly:boolean` param at this exact call layer. Whether the args are consumed earlier in the async state machine (this closure is nested two levels deep — `{{closure}}::{{closure}}` — so an outer frame may hold them) or via a different code path was not traced this pass. Recorded as an open question, not resolved either way.
- **Call-tree (dim3): PARTIAL.** 1.0.9 shape: `load_snapshot → load_local_state_synced → sync_local_runtime_state → load_local_state → OS reads` (depth 5). No 1.2.3-named function with the exact `load_local_state_synced` name was found in the `core/repository` file listing enumerated this pass (the closest disk-confirmed name is `sync_local_runtime_state_0x100a5f130.c`, which does exist) — candidate only, not traced.
- **Interface (dim4): PARTIAL.** Request params confirmed via ipc-contracts.jsonl: `argKeys: ["localOnly", "forceMetadata"]`. Response payload shape not re-extracted for 1.2.3 (1.0.9 had `AppStatusPayload{registry_items, account_summary, app_path_state, settings, service_state}` + warning array — not assumed to carry forward without independent 1.2.3 confirmation).
- **Errors:** Same `StateManager::try_get` null-panic guard and poisoned-mutex pattern visible in the wrapper as documented at 1.0.9.
- **Platform (dim5):** macOS confirmed only.
- **Acceptance (dim6):** missing.
- **Gate leaf:** consumerStartReady (startMode=reverse_guided, dim1 carried as accepted_unknown on the exact UI element only — the invoke command/args/owner chain itself is solid).

## Summary table

| Leaf | dim1 | dim2 | dim3 | dim4 | dim5 | dim6 | gate_tier |
|---|---|---|---|---|---|---|---|
| clean | pass | pass | partial (candidate leaves only) | partial (field names from frontend echo, unconfirmed on backend) | macOS-only declared | missing | consumerStartReady |
| rebuild_registry | **missing** | pass (strongest) | partial | missing | macOS-only declared | missing | **consumerStartBlocked** |
| load_snapshot | partial (accepted_unknown on exact UI element) | pass | partial | partial (request confirmed, response unconfirmed) | macOS-only declared | missing | consumerStartReady |

Cluster ceiling: **consumerStartReady is NOT cluster-wide** — 2 of 3 leaves reach it, 1 (`rebuild_registry`) is blocked on dim1 alone despite having the strongest dim2 evidence of the three. No leaf reaches strictImplementationUse or readyToImplement this pass (dim3/dim4/dim6 all partial-or-missing across the board).
