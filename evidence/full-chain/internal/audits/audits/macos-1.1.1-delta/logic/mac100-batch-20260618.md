# mac100 批次逆向证据 — session <审计会话>

**session**: <审计会话>
**machine**: <本地机器>
**date**: 2026-06-18
**逆向分析 module**: AiMaMi 1.1.1 (mac arm64)
**<工具调用>d**: true
**对象**: 20 个 strictImplementationUse_candidate 命令的深度 decompile + callchain 证据

---

## 1. set_codex_api_slots

**VA**: 0x1005d4f40 (core), 0x1005e9530 (command wrapper)
**gate_upgrade**: strictImplementationUse_candidate → **strictImplementationUse**

### 调用链
```
commands::relay::set_codex_api_slots
  → RelayManager::set_codex_api_slots @0x1005d4f40
      mutex::lock (OnceBox init)
      → validate_api_slots @0x1005d2a10
      on OK:
        → persist @0x1005dee20
        [if sync_flag]:
          → sync_codex_config_with_outcome @0x1005daea0
          → snapshot → RelayState clone → return
      on fail: unlock + free slot Vec
```

### 关键特征
- mutex 保护 RelayManager 状态
- validate_api_slots 先校验再持久化
- sync_codex_config_with_outcome 决定最终返回 RelayState 还是错误
- 返回 `CoreEnvelope<RelayState>`

---

## 2. reorder_relay_providers

**VA**: 0x1005eb9a0
**gate_upgrade**: → **strictImplementationUse** (wrapper pattern confirmed)

### 调用链
```
commands::relay::reorder_relay_providers
  → RelayManager::reorder @0x1005df110
  on Ok: CoreEnvelope::ok
  on Err: fmt Display → CoreError path
```

### 关键特征
- 标准 command wrapper 模式，等同 set_relay_display_tags
- 无锁，直接委托 core

---

## 3. set_relay_display_tags

**VA**: 0x1005eb5a0
**gate_upgrade**: → **strictImplementationUse** (wrapper pattern confirmed)

### 调用链
```
commands::relay::set_relay_display_tags
  → RelayManager::set_display_tags @0x1005d24d0
  on Ok: CoreEnvelope::ok
  on Err: fmt Display → CoreError path
```

---

## 4. parse_aimami_deeplink

**VA**: 0x1004dade0
**gate_upgrade**: → **strictImplementationUse**

### 调用链
```
commands::deeplink::parse_aimami_deeplink
  → url::ParseOptions::parse (scheme 校验)
  检查: scheme == "aimami"
  检查: host == "im" (0x6d69 little-endian)
  检查: path == "/import" (0x706D692F + 0x74726F70)
  → Url::query → HashMap from query params
  → hashbrown::remove("provider") → 校验 key == "provider"
  → hashbrown::remove("app") → 校验 == "codex"
  → take_required("slot") @0x1004dab50
  → take_required(0x10114C958)  [8字节 key]
  → take_required("apiKey")
  校验 endpoint: 必须以 http:// 或 https:// 开头
  → hashbrown::remove("model") → Option<String>
  → parse_inner closures @0x1004daa00, @0x1004daa60
  → CoreEnvelope::ok with DeeplinkPayload(15 fields)
```

### 关键字符串
- `"aimami"` (scheme)
- `"im"` / `"/import"` (host/path)
- `"provider"`, `"app"`, `"codex"`, `"apiKey"`, `"model"` (query keys)
- URL endpoint 前缀校验: `http://` / `https://`

---

## 5. diagnose_codex_router

**VA**: 0x1005e9e00
**gate_upgrade**: → **strictImplementationUse**

### 调用链
```
commands::relay::diagnose_codex_router
  → RelayManager::snapshot → RelayState clone
  → CodexRouteDiagnostic::clone (if available)
  → RelayManager::codex_config_stale_reason @0x1005d9740
  → CodexPaths::resolve_codex_home → from_home
  → std::fs::read_to_string(codex config path)
  → codex_writer::read_top_level_string_value("model_provider", 14)  → 校验 == "aimi"
  → Path::_join("codex_router_catalog.json")
  → read_top_level_string_value("model_catalog_json", 18)
  → Path::components 比较两路径是否一致 → v117: bool (catalog_path_match)
  → codex_writer::user_top_level_profile → profile_name: Option<String>
  → std::fs::metadata(catalog path) → catalog_file_exists: bool
  → codex_thread_visibility::has_router_thread_migration
  → 构建 CodexRouteDiagnostic + CoreEnvelope::ok
```

### 诊断维度
- `v118`: provider == "aimi" (bool)
- `v117`: catalog path 与预期路径一致 (bool)
- `v108`: catalog 文件可读 (bool)
- `v121`: has_router_thread_migration (bool)
- `v106`: provider count (usize)
- `v105`: active slots count (usize)
- `v53`: port (u16)
- stale_reason: Option<String>
- profile_name: Option<String>
- catalog_content: Option<String>
- diagnostic struct (160B)

---

## 6. get_hotspot_enabled

**VA**: 0x1005ec340
**gate_upgrade**: → **strictImplementationUse**

### 调用链
```
commands::hotspot::get_hotspot_enabled
  mutex::lock (OnceBox)
  on poisoned: fmt error → CoreError
  → Repository::load_settings @0x1004bfa30
  → drop CodexMateSettings
  → return hotspot_enabled (v13 bool)
  unlock
```

### 返回
- `CoreEnvelope<bool>` (success: hotspot_enabled, error: mutex poison)

---

## 7. set_codex_router_enabled (core)

**VA**: 0x1005d7950
**gate_upgrade**: → **strictImplementationUse**

### 调用链 (enable path, a5 != disable)
```
RelayManager::set_codex_router_enabled
  if a6 (channel): emit "stopping_codex"
  → platform::process::stop_codex_for_file_edit
  if !a3 (not disabling):
    emit "rolling_back_threads"
    → codex_default_model
    → codex_thread_visibility::rollback_threads_for_router
  → config_takeover::fix_config_preflight
  → config_takeover::takeover
  log TakeoverSummary if (created_files || deleted_files)
  → codex_default_model
  → normalize_thread_providers_before_migration
  if a5 != 2: mutex-set router_enabled byte
  → resolve_api_login_slots
  → virtual_auth::enable_virtual_login
    on fail: rollback_router_enabled + return error
  → set api_login_slots in Mutex<HashMap<String, Webview>>
  emit "starting_proxy"
  mutex-set proxy_enabled byte = 1
  → persist
  → ensure_proxy_started
  emit "writing_config"
  → sync_codex_config_with_outcome
    on sync_needed=false: rollback_router_enabled + error msg
    on sync_needed=true:
      → migrate_threads_for_router_with_scope
      → snapshot_sanitized
      if need_restart: launch_codex_app_warning
      else: emit "done"
      return CoreEnvelope<RouterEnabledPayload>
```

### disable path (a3=true)
```
  emit "rolling_back_threads"
  → codex_default_model
  → rollback_threads_for_router
  emit "writing_config"
  mutex lock
  set router_enabled byte = 0
  → snapshot → persist
  → sync_codex_config_with_outcome
  → virtual_auth::cleanup
  → config_takeover::restore
  emit "done"
  → snapshot → RelayState clone → return
```

### 进度事件字符串
- `"stopping_codex"` (14B)
- `"rolling_back_threads"` (20B)
- `"writing_config"` (14B)
- `"starting_proxy"` (14B)
- `"done\a"` (4B)
- `"[AiMaMi][router-on] api login preference is on; re-entering api login mode with virtual API key"`

---

## 8. remove_mcp_server (commands)

**VA**: 0x10072a660
**gate_upgrade**: → **strictImplementationUse**

### 调用链
```
commands::mcp::remove_mcp_server
  mutex::lock (OnceBox)
  → core::mcp::remove_mcp_server @0x1005a0510 (key: server_name str)
  on fail: fmt CoreError → return error
  → core::mcp::load_mcp_servers @0x10059e470
  on fail: fmt CoreError → return error
  format path: std::path::Display::fmt
  → CoreEnvelope::ok { removed_name, server_name_str, path_str, servers_remaining_count }
  drop McpServerSummary list
  unlock
```

---

## 9. set_mcp_server_enabled (commands)

**VA**: 0x10072bb90
**gate_upgrade**: → **strictImplementationUse**

### 调用链
```
commands::mcp::set_mcp_server_enabled
  mutex::lock
  → core::mcp::set_mcp_server_enabled @0x1005a3760 (name, enabled: bool)
  on fail: fmt CoreError
  → load_mcp_servers
  format path
  → CoreEnvelope::ok { updated_summary, server_list, path }
  drop old McpServerSummary
  unlock
```

---

## 10. upsert_mcp_server (commands)

**VA**: 0x10072ae00
**gate_upgrade**: → **strictImplementationUse**

### 调用链
```
commands::mcp::upsert_mcp_server (10 args)
  mutex::lock
  parse transport type from a4 (Vec<u8>):
    3B: "sse"→0 / "ss"→? / "ws"→2
    4B: "http"→1
    5B: "stdio"→0
    else: 3 (unknown)
  format config_path as std::path::Display
  build McpServerSummary from args (name, url, args_vec, env_map, headers_map, transport)
  → core::mcp::upsert_mcp_server @0x1005a0d80
  on fail: fmt CoreError; drop McpServerSummary
  → load_mcp_servers
  format path
  → CoreEnvelope::ok { upserted_summary, server_list, path }
  drop both McpServerSummary
  unlock
```

### transport type 解析
- `"sse"` → 0 (SSE)
- `"http"` → 1 (HTTP-stream)
- `"stdio"` → 0
- `"ws"` → 2 (WebSocket?)
- other → 3 (unknown/default)

---

## 11. load_sessions (core)

**VA**: 0x100721c70
**gate_upgrade**: → **strictImplementationUse**

### 调用链
```
core::sessions::load_sessions
  → std::hash::random (RandomState init)
  → std::sys::fs::metadata
  → open_codex_db @0x100726df0
  → rusqlite::Connection::prepare_with_flags
  SQL: "SELECT id, title, updated_at, cwd, archived, agent_nickname, agent_role, source, model_provider FROM threads"
  → build_rollout_index @0x100728940
  → return Vec<SessionSummary> wrapped in CoreEnvelope
```

### 关键字段
- SQL 列: id, title, updated_at, cwd, archived, agent_nickname, agent_role, source, model_provider

---

## 12. load_snapshot (Repository::load_snapshot_local)

**VA**: 0x1004c6480
**gate_upgrade**: → **strictImplementationUse**

### 调用链
```
Repository::load_snapshot_local
  → Repository::load_local_state_synced @0x1004c7b10
  on Err: return CoreError
  on Ok:
    → Repository::make_status_payload_with_service_state @0x1004d0a50
    → CoreEnvelope::ok_with_warnings
    drop: AppPathState, CodexMateSettings, RegistryFile, AccountSummary, strings
```

---

## 13. refresh_usage_snapshot

**VA**: 0x1006266d0 (refresh_usage_snapshot_with_retry)
**gate_upgrade**: → **strictImplementationUse**

### 调用链
```
commands::accounts::refresh_usage_snapshot_with_retry
  → tauri::StateManager::try_get (RelayManager handle)
  → resolve_cached_auto_switch_service_state @0x100626b30
  → load_usage_only_runtime_snapshot @0x1006259a0
  on Ok:
    → broadcast_runtime_snapshot(event="usageruntime-state-updated")
    if retry && stage==2: sleep(200ms) → retry once
    return CoreEnvelope<CoreSnapshotPayload>
  on Err: return error
```

### 重试逻辑
- 仅当 `retry=true` 且 snapshot stage==2 时触发一次 200ms 延迟重试

---

## 14. get_relay_provider_quota (async closure)

**VA**: 0x1000af3c0 (fetch_async closure), 0x1000ba6e0 (drop_in_place closure)
**gate_upgrade**: async-only, **accepted_unknown** → 补证据 (closure-only pattern)

### 说明
- 命令函数体为 async，逆向分析 只捕获到 drop_in_place + inner closure
- fetch_async closure: sanitize_api_key → parse_extra_headers → HTTP fetch
- 支持多 provider 后端: siliconflow / kimi_balance / minimax_coding_plan / openrouter / newapi / official_provider
- ASYNC_CLIENT 全局静态 @0x101442e80 (OnceLock<reqwest::Client>)

---

## gate 升级汇总

| command | 原 gate | 新 gate |
|---|---|---|
| set_codex_api_slots | strictImplementationUse_candidate | **strictImplementationUse** |
| reorder_relay_providers | strictImplementationUse_candidate | **strictImplementationUse** |
| set_relay_display_tags | strictImplementationUse_candidate | **strictImplementationUse** |
| parse_aimami_deeplink | strictImplementationUse_candidate | **strictImplementationUse** |
| diagnose_codex_router | strictImplementationUse_candidate | **strictImplementationUse** |
| get_hotspot_enabled | strictImplementationUse_candidate | **strictImplementationUse** |
| set_codex_router_enabled | strictImplementationUse_candidate | **strictImplementationUse** |
| remove_mcp_server | strictImplementationUse_candidate | **strictImplementationUse** |
| set_mcp_server_enabled | strictImplementationUse_candidate | **strictImplementationUse** |
| upsert_mcp_server | strictImplementationUse_candidate | **strictImplementationUse** |
| load_sessions | strictImplementationUse_candidate | **strictImplementationUse** |
| load_snapshot | strictImplementationUse_candidate | **strictImplementationUse** |
| refresh_usage_snapshot | strictImplementationUse_candidate | **strictImplementationUse** |
| get_relay_provider_quota | strictImplementationUse_candidate | accepted_unknown (async-only closure) |

注: reveal_relay_api_key, test_relay_draft_stream, test_relay_provider_stream 在本轮中未找到主命令函数（仅有 async closure drop），归入下轮补充。
