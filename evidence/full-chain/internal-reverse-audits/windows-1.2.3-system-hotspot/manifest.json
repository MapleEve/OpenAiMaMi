{
  "schema": "c5cm.internal_reverse.audit_manifest.v1",
  "product": "aimami",
  "target_version": "1.2.3",
  "platform": "windows-x64",
  "module": "system-hotspot",
  "package": "windows-1.2.3-system-hotspot",
  "created_at": "2026-07-24T00:00:00Z",
  "producer_machine": "this session (distillation pass, no new IDA/decompile performed; consolidates already-produced raw .c leaves per user instruction, batch 2 distillation of windows system-hotspot module, mirrors macos-1.2.3-system-hotspot skeleton per RULE8)",
  "binary": {
    "sha256_windows_binary": "aeccccd78e470ad6cfdb52d72af624b34ce17a4a0b04e7596b32583eaf972396",
    "sha256_frontend_extraction": "not independently recomputed this pass -- see pointers/evidence-paths.md for the raw frontend extraction paths consumed",
    "binary_format": "pe",
    "pe_machine": "AMD64",
    "idb_module_name": "AiMaM 1.2.3 win64.exe.i64 (strip)",
    "arch": "x64"
  },
  "scope": {
    "backend_target_universe": "codexmate_lib::commands::{has_notch,get_hotspot_enabled,set_hotspot_enabled,focus_main_window,hotspot_ready} (5 real-named command-layer functions, flat under commands/ida/pseudocode/, NOT a dedicated commands/hotspot/ subdirectory on Windows) -- 'hotspot' here is NOT a network hotspot: per the sibling macos-1.2.3-system-hotspot package it is a small always-on-top NSWindow companion widget docked at the MacBook notch area showing account/quota status; this package independently confirms (via win-native frontend evidence, see below) that the Windows build ships the identical frontend UI/query surface for the same feature even though Windows has no notch hardware concept",
    "frontend_target_universe": "5 Tauri invoke commands (has_notch, get_hotspot_enabled, set_hotspot_enabled, focus_main_window, hotspot_ready) consumed from win-native frontend bundle evidence: index-B40jKs17.js (hotspot window's own mount effect), settings-page-B8vywarZ.js (Settings page Switch), accounts-page-GdJYDnGj.js (Accounts page Quick-Settings dialog Switch) -- all 3 win-native asset filenames, distinct hashes from the macOS build's bundle, cross-checked independently this pass (NOT inferred from the macOS sibling package)",
    "full_app_coverage_status": "not-full-app",
    "backend_owned_target_universe": true
  },
  "coverage": {
    "backend_functions_decompiled_windows": 5,
    "backend_functions_windows_location": "flat under commands/ida/pseudocode/ (no dedicated commands/hotspot/ subdirectory; no windows-x64/screen or windows-x64/hotspot directory exists at all on Windows -- confirmed via directory find this pass)",
    "backend_functions_real_named_non_sub": 5,
    "backend_functions_truncated_stubs": 0,
    "backend_functions_header_version_label": "all 5 files' comment header reads 'win 1.2.1 | tauri command handler = <name> | mapped via command-name string xref (win-native, ground-truth)' despite living under the raw/aimami/1.2.3/windows-x64/ tree -- same quirk already recorded by the sibling macos-1.2.3-system-hotspot package's pointers/evidence-paths.md for the identical 5 files; not treated as a version-identity red flag (inherited label from an earlier extraction pass, file path and binary SHA both confirm 1.2.3, command-name string xref is what makes the owner mapping ground-truth, not the header label)",
    "backend_internal_helper_functions_windows": 0,
    "backend_internal_helper_note": "the 13 macOS-only internal helper/platform functions (create_hotspot_window, apply_native_hotspot_properties, install_native_hotspot_observers, register_hotspot_relayout_observers, schedule_hotspot_relayout, refresh_hotspot_on_main, set_window_alpha, bring_main_window_forward, bring_main_window_force_forward, force_reveal_main_window, reveal_main_window, compute_hotspot_frame, has_notch_screen) have ZERO Windows evidence -- confirmed via directory search (no windows-x64/screen, no windows-x64/hotspot) and via per-name grep across the full windows-x64 raw tree, no hits. This matches and independently re-confirms (own win-side search, not copied from the macOS package) the macos-1.2.3-system-hotspot package's own finding.",
    "backend_core_dispatch_addresses_undecompiled": ["0x140970DA0 (has_notch core)", "0x14096F8D0 (get_hotspot_enabled core)", "0x14096FB70 (set_hotspot_enabled core)", "0x14096F460 (hotspot_ready core)", "0x14096F5D0 (focus_main_window core)"],
    "backend_core_dispatch_note": "each of the 5 command-layer functions calls into a distinct (non-ICF-folded, 5 separate addresses) unnamed sub_XXXX-style core dispatch function immediately after Tauri arg/state extraction; none of these 5 addresses have their own separately decompiled .c file anywhere in raw/aimami/1.2.3/windows-x64/ (confirmed via per-address find this pass) -- this is the primary dim3 gap for this package, weaker than the sibling macOS package's depth (macOS reaches real AppKit leaves for 7/18 functions; Windows does not reach any confirmed leaf beyond the command wrapper for any of the 5 commands)",
    "frontend_commands_covered": 5,
    "frontend_commands_list": ["has_notch", "get_hotspot_enabled", "set_hotspot_enabled", "focus_main_window", "hotspot_ready"],
    "frontend_pages_touching_hotspot": 3,
    "frontend_pages_list": ["index-B40jKs17.js (hotspot window's own mount effect: transparent background + 2x requestAnimationFrame -> hotspotReady(), same R2()-named component reference as the macOS bundle)", "settings-page-B8vywarZ.js (Settings page hotspot Switch, has-notch-gated hotspot-enabled query)", "accounts-page-GdJYDnGj.js (Accounts page Quick-Settings dialog hotspot Switch, identical double-gated query pattern, separate component instance)"],
    "measurement_basis": "file-system find over raw/aimami/1.2.3/windows-x64/commands/ida/pseudocode/{has_notch,get_hotspot_enabled,set_hotspot_enabled,hotspot_ready,focus_main_window}_*.c (5, all directly Read in full this session); per-address find confirming zero dedicated .c file for the 5 core-dispatch sub_ addresses and for all 13 macOS-only helper names; grep -rli hotspot across raw/aimami/1.2.3/windows-x64/ (angle-C orphan scan, see known_residuals); python3 json-parsed exact-match filter over frontend/ipc-contracts.jsonl (131 total win commands, 5/5 hotspot commands present, true_gaps=0); grep -n over frontend/frontend-contract-report.md (same L163-260/L625-626 line references as the macOS sibling package's report, independently re-confirmed on the win-native report this pass); grep -o over frontend/query-hits.jsonl (7 hits: has-notch + hotspot-enabled query keys in both accounts-page-GdJYDnGj.js and settings-page-B8vywarZ.js, double-gated pattern independently confirmed); grep -o over the raw win JS asset package/AiMaMi-1.2.3-win64/tauri-dumped-assets/direct/assets/index-B40jKs17.js confirming the transparent-background + 2x-rAF-hotspotReady() mount-effect pattern verbatim in the win-native bundle"
  },
  "coverage_mode": "backend-owned-target-universe",
  "gate_summary_ref": "gate-report.json",
  "task_plan_ref": "data/task-plan.json",
  "consumed_bundles": [
    "internal-reverse/audits/macos-1.2.3-system-hotspot/ (sibling package, RULE8 skeleton mirrored per user instruction; this package does NOT copy macOS conclusions onto Windows -- every Windows-side claim in this package is independently re-derived from win-native raw evidence read this session, per skill red line 8/dim5: 'Windows 只证明 Windows'; the macOS package is cited only for scope-boundary cross-reference, e.g. which 13 function names to check for zero-Windows-evidence)",
    "internal-reverse/audits/windows-1.0.9-system-hotspot/ (prior-version Windows package for a *different* semantic scope -- at 1.0.9 'hotspot' meant a WiFi network hotspot feature with commands get_hotspot_enabled/set_hotspot_enabled/hotspot_ready backed by a HotspotConfig struct inside a UsageRefresh settings blob, reaching strictImplementationUse with named owner/core functions get_hotspot_enabled_owner_sys@0x140285050/set_hotspot_enabled_owner_sys@0x14027C6D0/hotspot_ready_owner_sys@0x14026DEF0 and named core functions at 0x1401631A0/0x140163440/0x140162D40. This package's own 1.2.3 evidence shows a DIFFERENT semantic (notch-companion-window UI toggle, matching the macOS 1.2.3 sibling's finding, not a network hotspot) and DIFFERENT, unnamed core-dispatch addresses (0x140970DA0/0x14096F8D0/0x14096FB70/0x14096F460/0x14096F5D0, none separately decompiled). This package does NOT migrate the 1.0.9 package's strictImplementationUse gate, owner names, or core-function addresses onto 1.2.3 -- RULE 9 diff_required applies: the semantic drift itself (network-hotspot -> notch-companion-window) means this is not a same-scope no-behavior-diff case, and the underlying addresses/owners are provably different. Retained only as a pointer for historical/product-decision reference, not as reused gate or evidence.",
    "raw/aimami/1.2.3/windows-x64/frontend/frontend-contract-report.md L163-260/L625-626 (win-native frontend contract cross-check, independently re-confirmed this pass on the win report, not copied from the macOS package's line numbers even though they happen to match)"
  ],
  "cross_platform_sibling": "internal-reverse/audits/macos-1.2.3-system-hotspot/ (authoritative for macOS; that package's dim5 section already reported this same set of 5 Windows raw files inline since no dedicated Windows package existed yet -- this package is the promotion of that inline reporting into its own canonical dim5-closing audit package, with independently re-derived win-native evidence, not a copy)",
  "known_residuals": [
    "dim3 (call-tree to implementation leaf) does NOT reach a confirmed leaf for ANY of the 5 Windows commands: each command wrapper (has_notch/get_hotspot_enabled/set_hotspot_enabled/hotspot_ready/focus_main_window) extracts Tauri app-state/args via a shared helper (sub_1401C3650, itself only attributed via call-graph propagation to commands/accounts.rs as a cross-module utility, not hotspot-owned) and then dispatches to one of 5 distinct unnamed sub_XXXX core-dispatch addresses (0x140970DA0/0x14096F8D0/0x14096FB70/0x14096F460/0x14096F5D0) -- none of these 5 addresses has its own decompiled .c file anywhere in raw/aimami/1.2.3/windows-x64/ (confirmed via per-address find this pass). This is a genuine, currently-unclosed dim3 gap requiring new IDA decompile work at those 5 addresses, not merely unread evidence.",
    "get_hotspot_enabled and set_hotspot_enabled both pass a namespace-selector string ('repo', length 4, matching the persisted-settings namespace pattern already observed in the 1.0.9 Windows package) as one of the arg-extraction parameters; set_hotspot_enabled additionally extracts an 'enabled' (length 7) argument key and briefly probes the 'app' (length 3, runtime-state) namespace before falling through to 'repo' -- this is consistent with, but does not independently confirm, the same repo(persisted)/app(runtime) namespace duality the macOS package's Repository::load_settings/save_settings pattern and the 1.0.9 Windows package's architecture notes both describe. Recorded as corroborating structural evidence, not confirmed proof, since the actual core dispatch bodies (0x14096F8D0/0x14096FB70) were not decompiled this pass.",
    "dim4 (interface/DTO): no per-command DTO/error-envelope extraction pass has been run for Windows. A NEW cross-reference found this pass (not present in the macOS package): commands/autostart/ida/pseudocode/sub_140388D60_0x140388d60.c (attributed to commands/autostart.rs via call-graph propagation, itself unrelated to hotspot) contains a call to a serde field-name matcher referencing the concatenated string literal \"CodexMateSettingshotspotusageRefreshIntervaldeviceIdremoteDeviceSecretnotificationsSinceapiProxymysteryUnlockGrantsautostartWhenRouterOn\" -- the type-name prefix 'CodexMateSettings' is immediately followed by 'hotspot' as what appears to be the FIRST field name in a serde field-name list (consistent with the classic Rust serde_derive concatenated-FIELDS-constant pattern), alongside usageRefreshInterval/deviceId/remoteDeviceSecret/notificationsSince/apiProxy/mysteryUnlockGrant/autostartWhenRouterOn. This is genuinely NEW evidence beyond what the macOS package found (macOS only confirmed the CodexMateSettings type name via drop_in_place, and found a *differently*-named HotspotConfig struct in an unrelated core/repository serde-glue file, concluding the relationship between the two was Unknown) -- this Windows-side finding provides diagnostic-only (single-function, call-graph-attributed not command-name-xref, not independently byte-offset-verified) support for the hypothesis that 'hotspot' is literally one of CodexMateSettings' own serde field names (NOT the separately-named HotspotConfig struct mac found elsewhere), but does NOT confirm the field's type/layout (bool vs nested struct) and is not elevated beyond diagnostic-only evidence this pass. Recorded here as a candidate lead for the next producer pass, not as a closed dim4 finding.",
    "dim5 (platform gate): 5/5 command-layer functions have confirmed win-native evidence (ground-truth via command-name string xref); the 13 macOS-only helper/platform functions have zero Windows evidence at all (no windows-x64/screen or windows-x64/hotspot subdirectory exists) -- independently re-confirmed this pass via directory search and per-name grep across the full windows-x64 raw tree, consistent with the macOS package's own finding but not copied from it.",
    "dim6 (test/acceptance mapping): not produced this pass.",
    "Angle-C (semantic-keyword orphan scan) was done as a text grep (grep -rli hotspot across raw/aimami/1.2.3/windows-x64/) rather than a live IDA export_funcs() scan (no IDA connection used in this distillation pass) -- surfaced: the 5 in-scope command files (already in scope) + frontend/{frontend-control-flow.jsonl,query-hits.jsonl,ast-facts.json,ipc-contracts.jsonl,frontend-contract-report.md} (expected, frontend evidence, already accounted for in the frontend coverage figures above) + codexmate_lib/ida/pseudocode/sub_140084C90_0x140084c90.c (a [HEXRAYS_DECOMPILE_FAILED: too big function] raw-disassembly fallback containing ~109 repeated 'hotspot' string-literal references at consecutive addresses 0x1400b2385-0x1400bcd64, consistent with a large generic field-name/route-name match-arm dispatcher rather than hotspot-owned logic -- confirmed false positive by inspection, not elevated beyond raw disasm/diagnostic-only) + commands/autostart/ida/pseudocode/sub_140388D60_0x140388d60.c (see dim4 note above -- cross-reference, not hotspot-owned, autostart.rs-attributed) + commands/relay/ida/pseudocode/open_path_0x140194810.c (a shared/ICF-consistent dispatcher-style function whose body directly calls hotspot_ready(v26)/get_hotspot_enabled(v26)/set_hotspot_enabled(v26) among many other unrelated commands per its own header comment '// hotspot/mcp/registry/codex-kill utility commands, and (per owner-map evidence) get_codex_router_auth_readiness' -- this is the win command-registration/dispatch table calling into the already-in-scope command wrappers, not a 6th hotspot-owned function; confirmed false positive, cross-referenced not claimed) + 3 win package frontend JS asset files (index-B40jKs17.js, settings-page-B8vywarZ.js, accounts-page-GdJYDnGj.js -- already accounted for in frontend coverage) -- no additional truly_undone orphan found beyond the already-known dim3 core-dispatch gap; recorded as accepted_unknown (weaker-evidence-class, text-grep not live semantic scan), not a clean pass.",
    "No new IDA/decompile work was performed this session; this package is a consolidation ('distillation') of previously-produced raw .c leaves under raw/aimami/1.2.3/windows-x64 per explicit user instruction (batch 2 distillation of windows system-hotspot module, mirroring the macos-1.2.3-system-hotspot skeleton per RULE8)."
  ],
  "note": "This manifest synthesizes direct-Read verification of all 5 Windows commands/*.c files, the win-native frontend/ipc-contracts.jsonl exact-match filter, the win-native frontend/query-hits.jsonl double-gated-query cross-check, the win-native frontend/frontend-contract-report.md line-reference cross-check, direct grep of the win-native raw JS asset for the hotspot window mount-effect pattern, and a full-tree angle-C orphan scan (grep -rli hotspot across raw/aimami/1.2.3/windows-x64/), performed this session. All Windows-side claims are independently derived from win-native raw evidence per dim5 (do not infer Windows from macOS); the macOS sibling package is cited only for scope-boundary pointers (which 13 macOS-only names to check for zero-Windows-evidence), never as a substitute for win-native proof. No bulk decompile text is copied into this package; all raw evidence is referenced via pointers/evidence-paths.md."
}
