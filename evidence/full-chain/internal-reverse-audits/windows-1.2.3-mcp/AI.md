# AI Handoff — windows-1.2.3-mcp

status: consumerStartReady (4/4 commands, wrapper-layer ground-truth); strictImplementationUse/readyToImplement NOT achieved; full_leaf_100_definition_v2 not closed (dim2 partial not strong, dim4/dim6 open, dim5 partial not closed)
evidence_root:  (hardcoded SMB absolute path per skill red line 16, verified mounted before every write this session)
versions: 1.2.3 (this package); historical baseline pointer only at internal-reverse/audits/windows-1.0.9-mcp/ (legacy schema, pre-dates full_leaf_100_definition_v2, smaller field set, NOT re-validated, not consumed as 1.2.3 evidence)
platforms: windows-x64 (this package); macOS 1.2.3 sibling package exists independently at internal-reverse/audits/macos-1.2.3-mcp/ (NOT used to infer Windows coverage per SKILL.md red line 8 -- cross-referenced only for diff narrative in SYSTEM-DIFF.md, all Windows claims below are grounded in Windows-only evidence read this session)
target_universe: codexmate_lib::core::mcp::* (16 raw .c files, 15 pre-existing + 1 newly captured this session) + 4 named commands::* mcp wrapper files (load_mcp_servers/upsert_mcp_server/set_mcp_server_enabled/remove_mcp_server, all ground-truth via command-name string xref) + frontend McpPage (shared cross-platform JS bundle, byte-identical filename confirmed present in the win package tree)

locator_audit:
  - angle-A (symbol enumeration): PARTIAL-LIVE -- unlike macos-1.2.3-mcp (no IDA connection at all), this session DID have a live, healthy win IDA MCP connection (server_health: status=ok, uptime=118766s, IDA decompiler_ready=true, module="AiMaMi.1.2.3 win64.exe", matches red line 17 requirements). A brute-force full-database disassembly-text search (search_text) timed out server-side after 60s and was not retried (retried once implicitly via the initial attempt+one retry with longer client timeout, per red line 17's "retry 1 then STOP" -- classified as inconclusive, not silently treated as "no gap"). A targeted find_regex('mcp_servers') on the fast strings-cache (strings_cache_ready=true, 29581 entries) DID complete quickly and returned exactly 6 string hits, each cross-checked via xrefs_to. This targeted approach discovered 1 previously-undumped function (upsert_mcp_server_0 @ 0x1409e86f0) that already carried a prior-session IDB comment naming it as the win match for mac core::mcp::upsert_mcp_server, but had never been written to raw/ or recorded in INDEX.jsonl. This function was decompiled live this session and captured as the 16th raw .c file. This is proof-positive that the original 15-address win-app-inventory-gapfill-leaf census (produced 2026-07-24T13:20:34Z) was NOT exhaustive for core::mcp. No full symbol-range func_query sweep filtered to core::mcp was completed (attempted func_query with a name-substring query but it appears to ignore the filter and return the full function table, which was not further pursued given time budget). Recorded as accepted_unknown (partial-live class) -- meaningfully stronger than macos-1.2.3-mcp's angle-A (no tool) but NOT a completed exhaustive live census.
  - angle-B (frontend IPC diff): DONE -- 4 frontend mcp-* commands from windows-x64/frontend/ipc-contracts.jsonl diffed 1:1 against the 4 named commands::*/ida/pseudocode/{load_mcp_servers,upsert_mcp_server,set_mcp_server_enabled,remove_mcp_server}_0x*.c wrapper files; true_gaps=0; command names identical to the macOS side (independently confirmed, not inherited).
  - angle-C (semantic orphan scan): PARTIAL -- find_regex('mcp_servers') across the full win 1.2.3 binary returned exactly 6 string hits; xrefs_to was run on all 6, revealing only 2 functions referencing them (editable_mcp_server_table candidate sub_1409E7A10, and the newly-found upsert_mcp_server_0) beyond the load_mcp_servers wrapper itself. This is a real but narrow-scope orphan scan (limited to functions referencing this one specific string family), NOT a full-tree keyword/symbol orphan sweep across the entire win 1.2.3 raw corpus (which macos-1.2.3-mcp's angle-C did perform, at the file-path grep level, for the macOS side). Recorded as partial, not DONE.
  - angle-D (manifest diff, 4-way classification): N/A this pass -- no prior windows-1.2.3-mcp manifest existed (first canonical package for this module+platform). No orphans requiring 4-way classification were found this pass (the 6 generic-glue functions among the 15 pre-existing files are classified inline in README.md/gate-report.json as "generic runtime/stdlib glue attributed via call-graph propagation", not as canonical-boundary orphans requiring already_in_canonical/local_outtake/different_machine_owner/truly_undone classification, since they were never independently claimed as mcp-domain business logic by this package).

coverage: 4/4 wrapper files read in full (ground-truth named, all confirm real command-name string xref); 16/16 core .c files present on disk this session (15 pre-existing + 1 newly captured), all opened, none show truncation markers (checked via head/tail + grep for "TRUNCATED"/"chars total" on every file, full-body read for the 8 smaller/medium files, structural spot-read + full head/tail read for the 2 largest files (sub_1409E1D30, 1284 lines; upsert_mcp_server_0, 800 lines with per-instruction address comments)); function-role identification confidence: 1 ground-truth (upsert_mcp_server_0, pre-existing IDB cross-platform signature comment) + 7 high-confidence inferred (via strong literal-string cross-evidence: unquote_toml/insert_mcp_block/parse_mcp_document/strip_toml_comment/replace_string_table/parse_mcp_section_header/editable_mcp_server_table) + 1 moderate-confidence inferred (sub_1409E1D30, transport-enum-parsing logic, not name-confirmed) + 6 generic-glue (not independently claimed as mcp business logic); 1/1 relevant frontend page section reused from macos-1.2.3-mcp package (shared cross-platform JS bundle, byte-identical filename confirmed present in win package tree, not re-derived)
coverage_mode: backend-owned-target-universe (full_app_coverage_status=not-full-app)
owner_resolution_status: PARTIAL -- 4/4 wrapper functions fully resolved (ground-truth, real names); core functions: 1/16 ground-truth-named, 7/16 high-confidence role-inferred, 1/16 moderate-confidence, 6/16 generic-glue (no mcp-specific role claimed), 6 of mac's 14 core functions have NO confirmed win counterpart at all (load_mcp_servers-core/quote_toml/prepare_existing_mcp_block/set_optional_string/remove_mcp_server-core/set_mcp_server_enabled-core)
full_app_coverage_status: not-full-app
per_target_required_results: see gate-report.json dim_status (dim1 closed, dim2 partial, dim3 partial, dim4 partial, dim5 partial, dim6 not-closed)
frontend_ccf_status: closed (reused from macos-1.2.3-mcp, shared cross-platform bundle; win-side wrapper ground-truth independently confirms the command<->handler mapping)
backend_ccf_status: partial (NOT "strong" like macos-1.2.3-mcp -- census-incompleteness proven, 6/14 mac-equivalent core functions unconfirmed on win)
pseudocode_status: 20/20 files (16 core + 4 commands) on disk, 0 truncation markers found, all opened this session
call_tree_status: partial -- upsert_mcp_server (the one command with full wrapper+core ground-truth chain) traced from wrapper argument extraction through core function identity confirmation, but the core function's full internal call tree to fs/toml leaves was NOT exhaustively traced this session (narrated at a structural level only in logic/FULL-CHAIN-1.2.3.md); the 7 high-confidence-inferred helper functions' call relationships ARE narrated (parse_mcp_document calls parse_mcp_section_header calls unquote_toml, etc.)
interface_status: partial -- upsert_mcp_server DTO field names (name/transport/enabled/command/args/url/headers/environment) read DIRECTLY as literal argument-extraction key strings from the wrapper pseudocode (ground-truth, stronger evidence than macos-1.2.3-mcp's inferred/key-length-match approach for this specific command), but still no formal per-field struct dump with defaults/nullability/error-envelope table; set_mcp_server_enabled (name/enabled) and remove_mcp_server (name) DTOs also ground-truth from wrapper literal keys
error_path_status: partial/narrative -- two named lock-acquisition failure paths visible in every wrapper ("repo"/"mutationgate" resource-string literals feeding a shared error-envelope trampoline sub_14047E370); core-level error paths (poisoned lock, IO errors, CAS-retry-exhausted equivalents) NOT independently traced for the win core implementation this session
boundary_status: partial for Windows itself (this package's own dim5 status) -- NOT extrapolated from or to macOS; macOS package's Windows-related "Unknown, unread" status is now superseded by this package for the win side specifically
gate_leaf_status: consumerStartReady=4/4 (wrapper-layer ground-truth basis, see README.md Backend Owner Confidence table for per-command core-layer caveats); strictImplementationUse=0/4; readyToImplement=0/4 (see gate-report.json and data/task-plan.json)
scripts: none beyond `find raw/aimami/1.2.3/windows-x64/mcp/ida/pseudocode -name '*.c' | wc -l` (=16, post-session) and `jq -r '.command' raw/aimami/1.2.3/windows-x64/frontend/ipc-contracts.jsonl | grep mcp` (=4) run this session; live IDA calls made via tools/IDA CLI win {server_health,find_regex,xrefs_to,decompile} (see pointers/evidence-paths.md for the exact sequence)

unknowns:
  - census completeness for core::mcp on win: NOT closed. 1 gap already found and closed this session (upsert_mcp_server_0); the discovery itself proves the method (call-graph-propagation-filtered gap-fill batch) is lossy; no full live symbol sweep was completed to bound the remaining gap. class: blocks_start (dim2/dim5 cannot close until this is resolved)
  - 6/14 mac core::mcp functions with no confirmed win counterpart (load_mcp_servers-core, quote_toml, prepare_existing_mcp_block, set_optional_string, remove_mcp_server-core, set_mcp_server_enabled-core): class: blocks_start
  - sub_1409E1D30 (1284 lines) identity: moderate-confidence inferred (transport-field parsing, related to upsert path) but not name-confirmed; class: accepted_unknown (partial-evidence class, not tool-unavailability class)
  - environment (win wrapper literal) vs env (macOS README field name) -- possible display-name alias vs real difference, not independently checked; class: accepted_unknown
  - "repo"/"mutationgate" win lock resource-string literals vs macOS's CodexMutationGate reason strings -- relationship not independently confirmed (could be the same gate at a different abstraction layer, or a different/additional lock); class: accepted_unknown
  - 6 generic-glue functions (2 Drop, 1 HashMap::insert, 1 BTree lookup, 1 Default-init, 1 thunk, 1 tag-check bool helper) attributed to mcp module via call-graph-propagation heuristic (>=2 same-module callers) -- heuristic is known to potentially mis-attribute generic monomorphized stdlib/crate-internal code when both observed callers happen to be in mcp; not independently checked against other modules' call sites; class: accepted_unknown
  - upsert_mcp_server_0's full internal call tree to real fs/toml leaves: not exhaustively traced (dim3 partial); class: blocks_start (for strictImplementationUse specifically)
  - dim4 formal per-field DTO struct dump (defaults/nullability/error-envelope table): not produced; class: blocks_start
  - dim6 test/acceptance mapping: not produced; class: blocks_start

do_not_infer:
  - Do not infer that the 16 core .c files on disk represent full coverage of codexmate_lib::core::mcp on win64 -- census incompleteness is proven, not merely suspected.
  - Do not infer that load_mcp_servers/set_mcp_server_enabled/remove_mcp_server have a confirmed win core-layer implementation just because their wrapper functions are ground-truth and some generically-related helper functions (editable_mcp_server_table, insert_mcp_block) were found -- those helpers are shared/common-path functions, not the specific per-command mutation/read implementations.
  - Do not infer Windows mcp completeness or field-set identity from the macOS package or vice versa; all claims in this package are grounded in windows-x64 evidence read this session, cross-referenced to macOS only in the explicitly-labeled Diff section.
  - Do not treat sub_1409E1D30 as a confirmed match for any specific mac-named function.
  - Do not extrapolate the legacy windows-1.0.9-mcp package's readyToImplement=true/full_leaf_100=true onto 1.2.3 -- that claim predates full_leaf_100_definition_v2, uses a different/smaller field set (no transport/url/headers/environment), and has not been re-validated.
