# AI Handoff — macos-1.2.3-system-hotspot

status: consumerStartReady (5/5 commands); strictImplementationUse/readyToImplement NOT achieved; full_leaf_100_definition_v2 not closed (dim3 mixed/open on closures, dim4 open, dim5 open for 13 macOS-only functions, dim6 open)
evidence_root:  (env: `${C5CM_INTERNAL_HISTORY_ROOT:-$C5CM_SHARE_ROOT}`); this session hardcoded the SMB absolute path per skill red line 16
versions: 1.2.3 only produced this pass; no prior-version (1.0.9/1.1.1) diff was independently re-verified -- those are separate INDEX.jsonl entries for a different version, not consumed as a baseline this pass (system-hotspot module has no equivalent 1.2.2 canonical bundle to diff against; NEW-delta markers are reused from the producing session's own IDA function-enumeration diff, not independently re-verified byte-level this pass)
platforms: macos-arm64 (strong, this package's primary evidence, 18/18 functions); windows-x64 (thin, 5 command-layer files only, reported inline in this package -- no separate windows-1.2.3-system-hotspot/ package exists)
target_universe: codexmate_lib::commands::hotspot::* (16 functions, single directory) + codexmate_lib::platform::screen::{compute_hotspot_frame,has_notch_screen} (2 functions, backend-owned-target-universe) + frontend 5 invoke commands consumed across 3 independent UI surfaces
locator_audit: angle-A (symbol enumeration) done via find|wc -l file-system count (18 macOS across 2 dirs, 5 Windows); angle-B (frontend IPC diff) done directly this pass via grep on ipc-contracts.jsonl -- true_gaps=0, all 5 commands map 1:1; angle-C (semantic orphan scan) done as a text-grep proxy (grep -rli hotspot across raw/aimami/1.2.3/macos-arm64), all hits manually triaged: the 18 in-scope files + 4 core/repository serde-glue false-positives (HotspotConfig only as a shared struct-name-list string, not hotspot-owned logic) + ~61 run/ida/pseudocode/*.c false-positives ('hotspot' as a route-name string substring, e.g. inside 'voice-search-resulthotspot') -- no truly_undone orphan found; angle-D (manifest diff) done -- 5/5 frontend commands covered, the other 13 of 18 functions are internal helpers called by the 5 command owners or by each other (already_in_canonical, not orphans)
coverage: 18/18 real-named macOS functions (16 commands/hotspot + 2 platform/screen), 0 truncated stubs, 0 sub_XXXX; 5/5 frontend commands reversed across 3 independent UI surfaces with full component-tree/query/mutation detail
coverage_mode: backend-owned-target-universe (full_app_coverage_status=not-full-app)
owner_resolution_status: resolved for all 18 macOS functions (real Rust-mangled names, 0 sub_XXXX); Windows resolved for 5/5 command-layer functions (ground-truth, command-name string xref per file header)
full_app_coverage_status: not-full-app
per_target_required_results: see gate-report.json dim_status (dim1 strong, dim2 strong, dim3 mixed -- 7/18 functions closed to real AppKit leaves, 11/18 stop at an undecompiled main-thread closure dispatch, dim4 not-closed, dim5 open for 13 macOS-only functions, dim6 not-closed)
frontend_ccf_status: strong (3 independent UI surfaces fully reversed: hotspot window's own React subtree in shared-index-app-frontend.md §1.2, Settings page Switch in mcp-sessions-settings-skills-frontend.md, Accounts page Quick-Settings dialog Switch in accounts-frontend.md; notable finding: the two Switch instances are fully independent React components with separate query-cache instances, not a shared global toggle store)
backend_ccf_status: strong-for-owner/mixed-for-leaf-depth (owner + non-failed pseudocode for 18/18 functions; but 11 of those 18 functions delegate their actual native AppKit work to an anonymous boxed-closure symbol dispatched via tauri_runtime_wry::send_user_message to the Tauri main-thread event loop, and that closure BODY is not present as a separately decompiled .c file in this evidence set -- see call_tree_status below)
pseudocode_status: done for 18/18 macOS addresses, 0 truncation; Windows done for 5/5 command-layer addresses, all name-confirmed (ground-truth per file header)
call_tree_status: mixed -- bring_main_window_forward, bring_main_window_force_forward, apply_native_hotspot_properties, set_window_alpha, create_hotspot_window's on-main-thread branch, compute_hotspot_frame, has_notch_screen (7 functions) terminate at real confirmed AppKit/Cocoa native leaves (NSApplication/NSRunningApplication activation, NSWindow property/frame/alpha setters, NSScreen auxiliary-area geometry). focus_main_window is ALSO now closed, but via CROSS-REFERENCE to the sibling package internal-reverse/audits/macos-1.2.3-window-path/ (discovered this session via the mandatory INDEX.jsonl append-time re-read -- see manifest.json known_residuals CORRECTION note), which is focus_main_window's canonical owner at strictImplementationUse (migrated from 1.0.9 baseline): closure resolved as WryUserEvent(discriminant=28, focused=true) via send_user_message. reveal_main_window, force_reveal_main_window, refresh_hotspot_on_main, register_hotspot_relayout_observers, create_hotspot_window's off-main-thread branch, and install_native_hotspot_observers' 5 NSNotificationCenter handler blocks (9 dispatch points remaining) still terminate at an anonymous closure symbol whose body was not independently decompiled this pass -- by structural analogy to the now-confirmed focus_main_window (same discriminant=28/dispatch shape, differentiated only by a boolean flag byte: focus_main_window/force_reveal_main_window use flag=1, reveal_main_window uses flag=0), there is materially stronger evidence that reveal_main_window/force_reveal_main_window post the same WryUserEvent(28, focused=<flag>) shape, and the RELAYOUT_VERSION debounce pattern suggests the notification handlers call schedule_hotspot_relayout -- but neither is confirmed by reading the actual closure body of THOSE functions specifically
interface_status: missing (dim4 not started; CodexMateSettings type-name confirmed via drop_in_place but field-level layout not decompiled; a separately-named HotspotConfig struct exists in shared core/repository serde-glue string lists with unconfirmed relationship to CodexMateSettings or to this module at all)
error_path_status: partial -- window_handle() kind-mismatch error paths observed in set_window_alpha/apply_native_hotspot_properties (returns tauri::Error for non-AppKit raw handle kinds), Mutex poisoned-lock error path observed in get/set_hotspot_enabled ("poisoned lock: another task failed inside"), thread-spawn-failure panic observed in schedule_hotspot_relayout ("failed to spawn thread") -- none extracted into a structured interfaces/ table this pass
boundary_status: partial -- macOS version gate confirmed (`>=12` + `pthread_main_np()==1` guards in has_notch_screen/compute_hotspot_frame); create_hotspot_window has a confirmed idempotent early-return when the window already exists; 13 of 18 macOS functions have zero confirmed Windows counterpart and no windows-x64/screen or windows-x64/hotspot directory exists at all -- plausible platform difference (no notch hardware on Windows), not confirmed fact
gate_leaf_status: consumerStartReady=5/5; strictImplementationUse=0/5; readyToImplement=0/5 (see gate-report.json and data/task-plan.json)
scripts: none required to reproduce coverage counts beyond `find raw/aimami/1.2.3/macos-arm64/commands/hotspot -name '*.c' | wc -l` (=16), `find raw/aimami/1.2.3/macos-arm64/platform/screen -name '*.c' | wc -l` (=2), and `grep -c '"command": "\(has_notch\|get_hotspot_enabled\|set_hotspot_enabled\|focus_main_window\|hotspot_ready\)"' .../ipc-contracts.jsonl` type file-system/grep counts documented in manifest.json
unknowns:
  - main-thread closure bodies (9 dispatch points across 5 functions + install_native_hotspot_observers' 5 handler blocks; focus_main_window's closure is RESOLVED via the window-path sibling cross-reference, no longer part of this open item): blocks_start (anonymous compiler-generated symbols, not separately decompiled; would need new IDA decompile work at the closure's own address)
  - CodexMateSettings field-level shape + relationship to HotspotConfig struct: blocks_start (no FieldVisitor::visit_str-style byte decoder found for either type this pass)
  - 13 macOS-only functions' Windows-side existence/absence: accepted_unknown (zero Windows evidence confirmed via directory search, consistent with but not confirmed as a genuine no-notch-on-Windows platform difference)
  - angle-C method quality (text-grep proxy, not live export_funcs()): accepted_unknown
  - dim6 test/acceptance mapping: blocks_start
do_not_infer:
  - Do not infer that the 6 main-thread-dispatched functions' behavior is confirmed just because a plausible inference (boolean-flag variant, notification-handler-calls-schedule_hotspot_relayout) is offered in logic/FULL-CHAIN-1.2.3.md -- those are inferences from call-site position and argument shape, not confirmed by reading the closure body.
  - Do not treat CodexMateSettings and HotspotConfig as confirmed to be the same struct, or confirmed to be different structs -- genuinely Unknown which (if either) backs get_hotspot_enabled/set_hotspot_enabled at the field level.
  - Do not extrapolate the 13 macOS-only functions' behavior onto Windows, and do not assume Windows genuinely lacks equivalent functionality just because no evidence exists -- both are open questions (accepted_unknown), not confirmed facts.
  - Do not treat the "1.2.3 NEW-delta" file-header markers on 5 functions as independently re-verified byte-level diffs by this pass -- they are reused from the producing session's own IDA function-enumeration diff classification.
  - Do not treat platform/screen's prior absence from any baseline document as evidence it was previously reviewed and found irrelevant -- it was genuinely never covered until this package (platform-baseline.md explicitly scoped only process/paths/debug_log/daemon/single_instance/system_volume/proxy).
