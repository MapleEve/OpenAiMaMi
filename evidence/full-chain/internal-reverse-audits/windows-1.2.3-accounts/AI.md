# AI Handoff — windows-1.2.3-accounts

status: consumerStartReady (4/12 accounts-domain commands); consumerStartBlocked (8/12); strictImplementationUse/readyToImplement NOT achieved; full_leaf_100_definition_v2 not closed (dim2 weak-mixed with 5 genuine Hex-Rays decompile failures + 1 unresolved owner, dim3 partial-asymmetric-but-cross-platform-confirmed, dim4 weaker-than-mac, dim5 win-native, dim6 open)
evidence_root:  (hardcoded SMB absolute path per skill red line 16; verified mounted this session via `ls`)
task_kind: distillation -- this session did NOT invoke IDA MCP tools (no decompile/xrefs_to/callees calls); it read pre-existing raw/aimami/1.2.3/windows-x64/{account_io,auth,oauth_login,oauth_refresh,commands/accounts,commands/system,codexmate_lib,repository,models,sessions}/ida/pseudocode/*.c files (already produced by an earlier session/pass, many carrying `WIN-1.2.1-MODULE-RECOVERY.md` methodology annotations) plus win's own frontend/{ipc-contracts.jsonl,frontend-control-flow.jsonl,ast-facts.json,frontend-contract-report.md}, and synthesized this canonical package as the Windows counterpart to macos-1.2.3-accounts
versions: 1.2.3 (binary + frontend); many individual .c files carry an internal `win 1.2.1` provenance comment indicating their module-attribution/naming work was originally done against the 1.2.1 win IDB and migrated into the 1.2.3 raw tree -- this package does NOT independently verify that migration's no-behavior-diff validity (see logic/DELTA-LOGIC-1.2.3.md)
platforms: windows-x64 (this package); sibling macos-1.2.3-accounts already exists and was used as the structural/wording template only, NOT as an evidence source for any win-specific claim
target_universe: 72 in-scope .c files -- 26 core (account_io 1 / auth 3 / oauth_login 12 / oauth_refresh 10) + commands/accounts 42 + 4 accounts-relevant named commands inside the catch-all codexmate_lib directory (export_accounts_to_file / import_accounts_from_file / preview_account_import / begin_chatgpt_oauth_login); core::repository (72 files) and core::models (3 files) cited as supplementary cross-reference, NOT counted in primary coverage; see manifest.json scope block
structural_gap_vs_mac: account_coordination and account_metadata core modules (10 and 7 files on macOS respectively) have ZERO files in the windows-x64 raw tree -- confirmed via `find`/`grep` for FileLock/AccountDomainLock/AccountSnapshotLock/fetch_subscription_metadata/try_lock_exclusive symbol names across the ENTIRE windows-x64 tree, 0 hits. This is a genuine evidence gap, not a scoping decision -- these modules' win-native functions, if they exist, are unattributed among the many `sub_XXXXXXXX` files this pass did not attempt to further resolve (would require live IDA MCP + the panic-Location/call-graph-propagation/command-name techniques documented in WIN-1.2.1-MODULE-RECOVERY.md, which this distillation pass did not run).
locator_audit: angle-A (symbol enumeration) done via find|wc -l on all in-scope win directories; angle-B (frontend command-name diff) done via win's OWN independently-extracted frontend/ipc-contracts.jsonl (131 total commands, matching mac's count) filtered by keyword, cross-checked against the byte-identical accounts-page-GdJYDnGj.js (SHA-256 09336d32548087c04de52882ad2b37d9236a26678e7b53483bd1dd22c3d10c55, verified identical between raw/aimami/1.2.3/macos-arm64/dumped/assets/ and raw/aimami/1.2.3/windows-x64/package/AiMaMi-1.2.3-win64/tauri-dumped-assets/direct/assets/ this session); angle-C (semantic orphan scan) NOT done; angle-D (manifest covered_commands vs full ipc-contracts.jsonl diff) done informally via keyword grep (20 candidates -> 12 in-scope after excluding auto-switch cluster and relay-adjacent commands, mirroring mac's exact same 12-command set by name), not a systematic diff script run
coverage: 72 in-scope .c files, 5 genuinely Hex-Rays-decompile-failed (disassembly-only stubs with `// [TRUNCATED]` + `[HEXRAYS_DECOMPILE_FAILED: switch analysis failed: bad target 0x... for case N]` headers -- all 5 share the identical root cause), 67/72 clean pseudocode; 1/1 accounts-relevant frontend page reused via byte-identical artifact (not independently re-reversed on win, but independently hash-verified + independently command-registry-confirmed)
coverage_mode: backend-owned-target-universe (full_app_coverage_status=not-full-app)
owner_resolution_status: MIXED -- some files (mostly in commands/accounts and the 4 codexmate_lib accounts commands, plus oauth_login/oauth_refresh/repository "named" files) carry real function names recovered via WIN-1.2.1-MODULE-RECOVERY.md's ground-truth techniques (tauri command-name string xref for command handlers = true value; panic-Location source-path xref for some core functions = true value); MOST files in account_io/auth (and the un-named majority of commands/accounts and repository) remain `sub_XXXXXXXX` with only DIRECTORY-level module attribution (via call-graph propagation, inferred not true-value) and NO semantic function naming -- this is NOT equivalent to mac's fully-named 141-file package and must not be reported as such
full_app_coverage_status: not-full-app
per_target_required_results: see gate-report.json dim_status (dim1 mixed-inherited-from-byte-identical-frontend, dim2 weak-mixed-with-5-IDA decompiler-failures-plus-1-unresolved-owner, dim3 partial-asymmetric-but-cross-platform-name-confirmed-for-repository-deep-leaves, dim4 weaker-than-mac-no-DTO-evidence, dim5 win-native-self-produced, dim6 not-closed)
frontend_ccf_status: mixed -- inherited from mac's already-reversed AccountsPage (raw/aimami/1.2.3/macos-arm64/frontend/pages/accounts-frontend.md, 147 lines) via PROVEN byte-identical artifact (SHA-256 match, not extrapolation) plus win's own independently-extracted ipc-contracts.jsonl (131 commands) and frontend-control-flow.jsonl (248 lines, contains account-keyword entries) as own-platform corroboration; same 8-confirmed/4-open command split as mac applies here by necessity (same JS bytes -> same interaction map), EXCEPT this package's overall consumerStartReady set is smaller than mac's because 5 of the 8 mac-frontend-confirmed commands additionally fail dim2 on win (Hex-Rays decompile failure) and 1 more (cancel_chatgpt_oauth_login) fails dim2 entirely (no wrapper found)
backend_ccf_status: weak-mixed -- 67/72 in-scope files have clean pseudocode (0 truncation), but the specific 5 files backing 5 of the 12 accounts-domain commands are genuinely Hex-Rays-decompile-failed (disassembly-only); repository (out-of-scope primary, cross-ref) has strong evidence with explicit cross-platform name-equivalence annotations for 8 key deep-leaf functions
pseudocode_status: 67/72 in-scope files clean, 5/72 Hex-Rays-failed (switch_account_and_restart_codex, refresh_single_account_usage, import_accounts_from_file, switch_account, refresh_usage_snapshot -- verified this session via direct grep for TRUNCATED/DECOMPILE-FAILED/chars-total markers across every in-scope directory, and by reading each failed file's header to confirm the `[HEXRAYS_DECOMPILE_FAILED: switch analysis failed: bad target 0x... for case N]` root cause, same pattern in all 5)
call_tree_status: partial-asymmetric-but-cross-platform-confirmed -- for the 4 consumerStartReady commands (remove_accounts/export_accounts_to_file/preview_account_import/begin_chatgpt_oauth_login) wrapper pseudocode is clean; remove_accounts's deep leaf (Repository::remove_accounts, win symbol remove_accounts_0) carries an explicit "win 1.2.3 | = mac codexmate_lib::core::repository::Repository::remove_accounts | 跨平台字符串签名匹配(名↔函数一致)" annotation confirming cross-platform equivalence to mac's already-documented deep leaf (SnapshotQuarantine two-phase quarantine-then-purge pattern per macos-1.2.3-accounts/logic/FULL-CHAIN-1.2.3.md). Several OTHER repository functions (switch_account_0, rollback, recover_account_removal_quarantine, remove_account_backup_files, persist_registry, hashed_account_snapshot_path, auto_switch_config) carry the same kind of annotation but back commands that are themselves dim2-blocked on win (e.g. switch_account_0 backs switch_account/switch_account_and_restart_codex, both dim2-failed at the wrapper level) -- this package conservatively does NOT promote those commands to consumerStartReady despite the strong deep-leaf evidence, because the wrapper's own decompile failed (see README.md Action/Non-action)
interface_status: weaker-than-mac -- win core::models has only 3 files (error_hint, normalize_relay_models, 1 unnamed), NONE are Account*/Logout*/Remove*/Switch*/OauthLogin* DTO serde glue (mac has 175 files including all of these). Only evidence available is win's own ipc-contracts.jsonl argKeys (parameter names only, e.g. remove_accounts->accountKeys, switch_account->accountKey, export_accounts_to_file->targetPath+accountKeys, import_accounts_from_file->filePath+expectedContentSha256+overwriteExisting+selectedKeys, preview_account_import->filePath, load_snapshot->localOnly+forceMetadata, refresh_single_account_usage->accountKey) -- this is genuinely win-native (extracted from win's own JS bundle instrumentation) but is parameter-name-level, not struct-field-level
error_path_status: missing (same as mac's dim4 gap, but win has even less supplementary evidence since core::models lacks DTO glue; only known destructive-op pattern is the repository two-phase quarantine-then-purge, cross-platform-confirmed by name only, body not independently re-read this pass beyond a headers-and-line-count check)
boundary_status: win-native-only this pass -- all primary evidence (raw pseudocode + ipc-contracts.jsonl + tauri-dumped-assets) is win's own artifact; frontend interaction semantics are reused from mac via PROVEN byte-identical file (not extrapolated); macOS behavior is NOT assumed for any win-only-unresolved item (account_coordination/account_metadata absence, cancel_chatgpt_oauth_login owner, the 5 Hex-Rays failures)
gate_leaf_status: consumerStartReady=4/12; consumerStartBlocked=8/12; strictImplementationUse=0/12; readyToImplement=0/12 (see gate-report.json and data/task-plan.json)
scripts: none beyond file-system checks run this session: `find <dir> -name '*.c' | wc -l` per in-scope directory; `grep -rl 'TRUNCATED\|DECOMPILE-FAILED\|chars total'` per in-scope directory; `grep -rl <symbol-names>` across the full windows-x64 tree to confirm account_coordination/account_metadata/cancel_chatgpt_oauth_login absence; `shasum -a 256` cross-check of accounts-page-GdJYDnGj.js (mac dump vs win tauri-dumped-assets) and of the win binary itself (matches windows-1.2.3-relay/manifest.json's recorded sha256_binary aeccccd78e470ad6cfdb52d72af624b34ce17a4a0b04e7596b32583eaf972396); python3 one-liner to parse win's own ipc-contracts.jsonl for accounts-domain command candidates
unknowns:
  - account_coordination/account_metadata win-native evidence: blocks_start (genuinely absent from the raw tree; would require a fresh live-IDA win-native module recovery pass using WIN-1.2.1-MODULE-RECOVERY.md's own techniques, targeted at these two modules specifically)
  - cancel_chatgpt_oauth_login backend wrapper: blocks_start (not found by name anywhere in windows-x64; likely one of the 18 tauri commands WIN-1.2.1-MODULE-RECOVERY.md reports as NOT precisely nameable via string-pool xref or disambiguated by 2-function collision, but this package did not verify which of the 18 it is)
  - 5 Hex-Rays-decompile-failed command wrappers: blocks_start (genuine tool-ceiling this pass; red line 13 requires attempted breakthrough techniques before any accepted_unknown claim, none attempted -- no live IDA MCP access this distillation pass)
  - dim4 field-level DTO extraction: blocks_start (worse than mac -- no win-native DTO glue exists in the 3-file core::models to extract from; would require either a win-native models-recovery pass or an explicit product decision to treat mac's DTO shapes as the implementation basis with a documented platform-parity assumption, which this package does NOT make)
  - dim6 test/acceptance mapping: blocks_start (not produced)
  - RULE9 migration validity of the 1.2.1-provenance-comment files being reused as 1.2.3 evidence: blocks_start (this package did not independently verify no-behavior-diff between 1.2.1 and 1.2.3 win binaries for these functions; the migration is real but its own basis is a prior pass's implicit assumption, not this pass's verified fact)
do_not_infer:
  - Do not infer that this package's 4/12 consumerStartReady count means the other 8 commands don't exist or aren't implementable on Windows -- 5 have real (if disassembly-only) evidence of existing, 1 (cancel_chatgpt_oauth_login) is a confirmed-present frontend command whose backend owner is simply unresolved this pass, and 2 (load_snapshot/logout) have fully clean backend pseudocode but an unconfirmed frontend call-site (same finding as mac, inherited via byte-identical file).
  - Do not infer that the "win 1.2.3 | = mac X | 跨平台字符串签名匹配" annotations on repository functions constitute independent dim3 closure for the commands whose OWN wrapper decompile failed (switch_account, switch_account_and_restart_codex, refresh_usage_snapshot) -- per the 2026-05-27 Strict Leaf Readiness Clarification, backend owner/IDA decompile must be non-failed FOR THE SAME TARGET, and a strong deep-leaf substitute does not promote a target whose own wrapper is decompile-failed.
  - Do not treat the account_coordination/account_metadata absence as evidence these capabilities don't exist on Windows -- treat as blocks_start, not as a product fact.
  - Do not extrapolate any win-only-unresolved item from macOS. Do not treat the byte-identical-frontend reuse as equivalent to "win frontend independently re-reversed" -- it is a proven-identical-artifact reuse, explicitly weaker than an independent re-reversal and should be labeled as such if cited elsewhere.
  - Do not treat this package as having closed strictImplementationUse or readyToImplement for any of the 4 consumerStartReady commands -- dim4 (DTO) is weaker than mac's already-incomplete dim4, and dim6 is fully open.

## Next producer steps (if a future pass wants to close more gates)

1. Run a live-IDA win-native module recovery pass (per WIN-1.2.1-MODULE-RECOVERY.md's three-layer method) specifically targeting account_coordination/account_metadata candidate functions and the 18 previously-unnamed tauri commands to determine whether cancel_chatgpt_oauth_login is among them and, if so, name it.
2. Attempt red-line-13 breakthrough techniques (retry decompile with different IDA settings, examine the jump-table bytes manually, or accept a caller-disambiguation-based partial reconstruction with explicit `recovery_attempts` logging) on the 5 Hex-Rays-decompile-failed files before any accepted_unknown claim.
3. If a win-native core::models equivalent module exists but wasn't captured in this raw tree, locate and read it to close dim4; otherwise consider a targeted account-domain-only DTO field-level pass over the win binary directly.
4. Verify the RULE9 migration validity of the many `win 1.2.1`-provenance files being reused as 1.2.3 evidence -- either via live IDA diff against the 1.2.1 IDB or by documenting an explicit accepted-migration decision with reasoning.
5. Run a dim6 pass mapping the 4 consumerStartReady win commands to Polaris's own C5 unit/E2E test surface.
6. Run angle-C (semantic-keyword orphan scan) across all win directories for account/snapshot/registry/lock/oauth keywords outside the current in-scope set.
