# AI Handoff — macos-1.2.3-daemon-autoswitch

status: partial evidence pack (reduce-only pass); blocks_start for all 13 canonical commands (0/13 consumerStartReady/strictImplementationUse/readyToImplement); NOT a migrated_no_behavior_diff from 1.0.9 (8 functions confirmed 1.2.3 NEW-delta)
evidence_root:  (env: `${C5CM_INTERNAL_HISTORY_ROOT:-$C5CM_SHARE_ROOT}`); this session hardcoded the SMB absolute path per skill red line 16
versions: 1.2.3 (compared against 1.0.9 baseline for canonical-universe origin; also cross-checked 1.2.2-vs-1.2.3 NEW-delta provenance tags embedded in raw file headers)
platforms: macos-arm64 (this package); windows-x64 sibling at ../windows-1.2.3-daemon-autoswitch/ (produced same day, independently weaker dim2 coverage, same conservative gate posture)
target_universe: 13 canonical commands (daemon 6: run_daemon_once/load_bootstrap_state/note_usage_refresh_activity/schedule_full_runtime_refresh/start_usage_refresh_watcher/update_usage_refresh_schedule + auto-switch 7: start_auto_switch_pending_watcher/load_pending_auto_switch/confirm_pending_auto_switch/dismiss_pending_auto_switch/confirm_pending_auto_switch_and_restart_codex/configure_auto_switch/set_auto_switch), inherited from ../macos-1.0.9-daemon-autoswitch/, cross-validated against ../windows-1.2.3-daemon-autoswitch/ same-version-sibling's identical 13-name list
locator_audit: angle-A (exact-filename existence check per canonical command name) done: 9/13 exact match + non-truncated (brace-balance + marker scan, 0 flagged), 1/13 confirmed zero raw evidence anywhere in the macOS 1.2.3 tree, 3/13 no exact match but a plausible-unconfirmed name-adjacent candidate each; angle-B (frontend ipc-contracts.jsonl diff per command) done: 8/13 hits, 5/13 zero hits consistent with 1.0.9 native-thread pattern; angle-C (semantic orphan scan beyond the 13-name list) NOT done this pass; angle-D (manifest/gate diff against 1.0.9 + windows sibling, 4-way classification) done via provenance-tag reading (基线 same-set / 1.2.3 NEW-delta / no-tag) -- see gate-report.json unknowns
coverage: 9/13 commands have exact-command-name-matched, non-truncated pseudocode; 1/13 (schedule_full_runtime_refresh) zero raw evidence; 3/13 (run_daemon_once/confirm_pending_auto_switch/confirm_pending_auto_switch_and_restart_codex) name-mismatch with unconfirmed candidate. 8 functions in the underlying core::repository/commands support layer carry a genuine header-embedded "1.2.3 NEW-delta" tag (real behavior change vs 1.2.2, consistent with the "account switching stability" changelog item cross-referenced from ../macos-1.2.3-relay/README.md).
coverage_mode: backend-owned-target-universe (full_app_coverage_status=not-full-app)
owner_resolution_status: resolved (exact-name match) for 9/13; unresolved (candidate-only, unconfirmed identity) for 3/13; not-applicable (zero evidence) for 1/13
full_app_coverage_status: not-full-app
per_target_required_results: see gate-report.json dim_status (dim1 mixed-and-stronger-than-windows-sibling, dim2 strong-for-9/absent-for-1/ambiguous-for-3, dim3 not-closed, dim4 not-closed, dim5 closed-for-existence-both-platforms/open-for-parity, dim6 not-closed)
frontend_ccf_status: mixed -- 5/13 commands (configure_auto_switch, set_auto_switch, load_pending_auto_switch, dismiss_pending_auto_switch, confirm_pending_auto_switch_and_restart_codex) have real component-level trace in already-extracted page docs (accounts-frontend.md, shared-index-app-frontend.md); 3/13 (run_daemon_once, load_bootstrap_state, confirm_pending_auto_switch-plain) have wrapper-registration-only evidence with no traced caller; 5/13 have zero frontend evidence at all, consistent with (not independently re-verified against) the 1.0.9 native-thread pattern
backend_ccf_status: strong-for-9, zero-for-1, ambiguous-for-3 -- see coverage line above
pseudocode_status: done and genuinely non-truncated for 9/13 addresses at exact-name match (verified via python brace-balance + TRUNCATED-marker scan across all 59 scoped files this pass, 0 flagged); zero for schedule_full_runtime_refresh; candidate-only for the other 3 (run_daemon_once_blocking / confirm_pending_auto_switch_async / perform_switch_payload_with_restart -- the last one is notably a genuinely-fully-decompiled 1360-line NEW-delta function via large-body server-side paging, not a truncation stub, but its command-identity mapping is unconfirmed)
call_tree_status: not-produced -- no dim3 work performed this pass (reduce-only, no IDA callees/xrefs_to calls)
interface_status: missing (dim4 not started for 1.2.3; ../macos-1.0.9-daemon-autoswitch/interfaces/{autoswitch-commands.md,daemon-commands.md} exist as STRUCTURAL REFERENCE ONLY, not 1.2.3 evidence, especially for the 8 confirmed-NEW-delta functions which postdate that 1.0.9 analysis)
error_path_status: missing (bundled into dim4 gap)
boundary_status: partial -- platform existence confirmed independently on both macOS (this package) and Windows (sibling) from their own respective 1.2.3 binaries; parity/behavior-equivalence between the two platforms NOT assessed this pass (dim5 open-for-parity)
gate_leaf_status: consumerStartReady=0/13; strictImplementationUse=0/13; readyToImplement=0/13 (see gate-report.json and data/task-plan.json) -- DELIBERATE conservative posture mirroring ../windows-1.2.3-daemon-autoswitch/'s reading of RULE7's 9-sub-dimension bar, even though this package's dim1 evidence is independently stronger for 5 commands; a looser precedent exists in ../macos-1.2.3-relay/ (consumerStartReady granted on dim1(wrapper)+dim2(strong) alone) which this package deliberately did NOT follow, prioritizing same-module cross-platform consistency instead
scripts: python brace-balance + TRUNCATED-marker scan (ad-hoc, not checked into this package -- reproducible via: for each .c file, count '{' vs '}' and grep for 'TRUNCATED'/'chars total'/'truncat'); exact-filename existence check via `find <dir> -iname "<command>_0x*.c"` per canonical command name; frontend hit-count via `grep -c "\"command\": \"<name>\"" ipc-contracts.jsonl`
unknowns:
  - run_daemon_once <-> run_daemon_once_blocking(0x1007c8fe0) identity: blocks_start (plausible, unconfirmed)
  - confirm_pending_auto_switch <-> confirm_pending_auto_switch_async(0x1000eac80) identity: blocks_start (plausible, unconfirmed); ALSO whether the plain variant is dead frontend code in 1.2.3 is itself unresolved
  - confirm_pending_auto_switch_and_restart_codex <-> perform_switch_payload_with_restart(0x1006a1800) identity: blocks_start (plausible by name/timing/changelog proximity, unconfirmed; windows sibling independently found a DIFFERENT unconfirmed candidate for the same command -- platforms have not converged)
  - schedule_full_runtime_refresh existence at 1.2.3: product_decision (zero raw evidence, not independently IDA-verified as genuinely absent vs not-yet-targeted)
  - 5 zero-frontend-hit commands' dim1 native-thread-substitute status at 1.2.3: product_decision (consistent with 1.0.9 pattern via weak grep-only cross-check, not IDA-verified)
  - dim3/dim4/dim6 for all 13 targets at 1.2.3: blocks_start (not produced this pass; 1.0.9 references are structural-template-only, not evidence, especially for 8 NEW-delta functions)
do_not_infer:
  - Do not infer that ../macos-1.0.9-daemon-autoswitch/'s readyToImplement=true/full_leaf_100=true carries forward to 1.2.3 -- 8 functions are confirmed genuinely new (1.2.3 NEW-delta tag), and the remaining baseline-same-set functions have not been independently re-verified against the 1.2.3 binary SHA this pass.
  - Do not infer Windows daemon-autoswitch completeness or identity resolutions from this macOS package, or vice versa -- see ../windows-1.2.3-daemon-autoswitch/ which independently found a DIFFERENT candidate name for the confirm_pending_auto_switch_and_restart_codex identity question.
  - Do not treat the 3 name-adjacent candidates (run_daemon_once_blocking / confirm_pending_auto_switch_async / perform_switch_payload_with_restart) as confirmed identity matches for their respective canonical commands -- all 3 are plausible-but-unconfirmed.
  - Do not treat ../macos-1.2.3-relay/'s looser consumerStartReady-on-dim1+dim2-alone precedent as applicable here -- this package deliberately chose the stricter, same-module-cross-platform-consistent reading instead.
