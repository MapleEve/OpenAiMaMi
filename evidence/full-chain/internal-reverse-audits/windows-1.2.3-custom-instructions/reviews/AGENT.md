# Codex/Agent 独立复核 — windows AiMaMi 1.2.3 custom-instructions

Evidence path used: same as `reviews/CLAUDE.md` (shared evidence set; this file records an independent second pass over the same artifacts, not a separate re-derivation).

Coverage confirmed: 5/5 commands cross-checked against `internal-reverse/audits/windows-1.0.9-custom-instructions/manifest.json`'s `commands` array — identical set, identical order not required but identical membership confirmed.

Cross-version corroboration: `internal-reverse/REVERSE-STATUS.md` wf-aimami118-delta-20260711 entries independently already classified this same 5-command family (as part of a wider 39/42-command voice+custom_instruction cluster) as a persistent, pre-existing, cross-version, cross-platform gap through 1.0.9->1.1.1->1.1.8 on both macOS and windows — this bundle's 1.2.3 windows finding is consistent with, and extends, that established pattern rather than contradicting it.

Gaps: same as CLAUDE.md — macOS 1.2.3 Unknown, 2 inherited 1.0.9 Unknowns unchanged.
