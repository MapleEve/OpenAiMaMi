# AI Handoff — windows-1.2.3-maintenance

status: partial (neither command reaches consumerStartReady); readyToImplement/strictImplementationUse NOT achieved; full_leaf_100_definition_v2 not closed
evidence_root:  (SMB absolute path hardcoded per skill red line 16)
versions: 1.2.3
platforms: windows-x64 (this package, backend-only; no independent frontend re-reversal this pass)
task_kind: reduce/distillation -- this session did NOT invoke IDA MCP tools (no decompile/xrefs_to/callees calls); it read pre-existing raw/aimami/1.2.3/windows-x64/{commands,repository}/ida/pseudocode/*.c files plus frontend/{ipc-contracts.jsonl,frontend-control-flow.jsonl} and synthesized this package. No new decompilation, no implementation code produced (per task scope).
target_universe: 3-command "maintenance / account-registry" domain named by task: rebuild_registry, clean, load_snapshot. load_snapshot is EXPLICITLY OUT of this package's primary coverage -- it is already an independently-produced leaf inside ../windows-1.2.3-accounts/ (one of that package's 12 accounts-domain commands, gate status consumerStartBlocked there for dim1/uiCallEntryReady reasons). This package's OWN primary scope is {rebuild_registry, clean} only, to avoid red-line-5 duplicate producer work.
locator_audit: angle-A (IDA symbol enumeration) NOT performed this pass (no live IDA MCP access, reduce-only task); angle-B (frontend ipc-contracts.jsonl command diff) done via grep -- both rebuild_registry and clean confirmed present as tauriInvokeWrapper entries AND as frontend-control-flow.jsonl entries, both classed "minified-static-function" trigger type with element=null (API-wrapper-only evidence, no confirmed DOM/component call-site); angle-C (semantic orphan scan) NOT done; angle-D (manifest covered_commands vs full command universe diff) done narrowly for this 3-command set only, not a full-module scan
coverage: rebuild_registry has 3 raw .c files (0 truncation markers, confirmed via grep across all 3), forming a 3-hop call chain: commands/rebuild_registry_0x14016c220.c (wrapper) -> repository/rebuild_registry_with_policy_0x14039f840.c (deep-leaf, 2095 lines, cross-platform-signature-annotated) -> repository/persist_registry_0x14038fb30.c (called at rebuild_registry_with_policy.c line 1383: `persist_registry(v290, v65, (__int64)&v275, 1)`). clean has ZERO raw .c files anywhere in raw/aimami/1.2.3/windows-x64/ (confirmed via find -iname and grep -rl for function-name-adjacent strings aClean/clean_handler/clean_impl across the whole tree) -- backend owner completely unresolved, same evidentiary class as windows-1.2.3-accounts's cancel_chatgpt_oauth_login (frontend-confirmed-present, zero backend evidence).
coverage_mode: backend-owned-target-universe (full_app_coverage_status=not-full-app)
owner_resolution_status: rebuild_registry -- resolved via ground-truth command-name string xref (wrapper) + cross-platform string-signature match (deep leaf, deep-leaf-2). clean -- UNRESOLVED (zero candidate files found).
full_app_coverage_status: not-full-app
per_target_required_results: see gate-report.json dim_status and per_command (dim1 weak-both -- API-wrapper-only evidence, no confirmed UI element call-site for either command; dim2 strong-for-rebuild_registry/zero-for-clean; dim3 partial-for-rebuild_registry-only -- 3-hop chain confirmed, terminal leaf beyond persist_registry not further traced this pass; dim4 partial-for-rebuild_registry-only -- no-params request confirmed, one error string confirmed, full response DTO shape not extracted; dim5 win-native-self-produced for rebuild_registry, N/A for clean (no evidence to be platform-gated); dim6 not-closed for either)
frontend_ccf_status: weak-for-both -- frontend/frontend-control-flow.jsonl has one entry each for "clean" and "rebuildRegistry", both classed `trigger.type=minified-static-function` with `element:null`, meaning the extraction pipeline found the JS function definition (`re("clean")` / `re("rebuild_registry")` inside the shared `Ee` API object at index-B40jKs17.js:87) but did NOT trace a real DOM/React component click-handler chain to it. This is evidentiarily WEAKER than a confirmed UI trigger and should not be read as "button exists and is wired" -- only "the API surface exists in the bundle".
backend_ccf_status: rebuild_registry strong (3/3 files clean pseudocode, cross-platform-annotated); clean zero (0/0 -- no files exist to assess)
pseudocode_status: rebuild_registry -- all 3 files pass a truncation-marker grep (0 hits for TRUNCATED/HEXRAYS_DECOMPILE_FAILED/chars-total) across all 3; genuinely complete Hex-Rays output, not stubs. clean -- N/A, no files.
call_tree_status: rebuild_registry -- 3-hop chain confirmed by direct grep cross-reference (wrapper calls into rebuild_registry_with_policy via inlined dispatch pattern typical of this binary's async-command machinery; rebuild_registry_with_policy directly calls persist_registry at a fixed call site). Does not extend past persist_registry this pass (persist_registry's own internal writes -- e.g. the eventual file I/O syscall -- not traced to OS-call terminal this pass, unlike the 1.0.9 baseline's fuller 6-hop trace). clean -- no call tree, no evidence.
interface_status: rebuild_registry -- request confirmed no-params (bare invoke); one error path confirmed (3-retry-then-error "auth.json ownership kept changing while rebuilding the account registry", 71-byte string literal at repository.rs); full response DTO shape NOT extracted this pass (would require reading persist_registry's own return-path construction and any downstream serializer, not done). clean -- no evidence, no interface derivable from 1.2.3 raw tree.
error_path_status: rebuild_registry -- partial (1 error string confirmed as above; other branches not enumerated). clean -- missing entirely.
boundary_status: rebuild_registry -- win-native-only, not extrapolated from macOS (the cross-platform-signature annotations found IN the .c file headers are the prior producer's own claims, re-cited here, not independently re-verified byte-for-byte this pass). clean -- Unknown, correctly not inferred from either the 1.0.9 Windows evidence or any macOS 1.2.3 evidence (no macos-1.2.3-maintenance package exists to cross-check against; not searched this pass).
gate_leaf_status: consumerStartReady=0/2 (this package's 2-command primary scope); rebuild_registry blocked-on-dim1 (uiCallEntryReady not satisfied, same standard as windows-1.2.3-accounts applied to load_snapshot/logout); clean blocked-on-dim1-AND-dim2 (worst case in this package). load_snapshot's gate status is NOT re-derived here; see ../windows-1.2.3-accounts/gate-report.json (consumerStartBlocked, same file, dim2 clean/dim1 gap reason).
scripts: `find raw/aimami/1.2.3/windows-x64 -iname '*clean*' -o -iname '*registry*' -o -iname '*snapshot*'` (module discovery); `grep -rl 'TRUNCATED\|HEXRAYS_DECOMPILE_FAILED\|chars total'` against the 3 rebuild_registry files (0 hits, confirms clean pseudocode); `grep -n persist_registry repository/ida/pseudocode/rebuild_registry_with_policy_0x14039f840.c` (confirms call-tree hop 2->3); `grep` against frontend/ipc-contracts.jsonl and frontend/frontend-control-flow.jsonl for clean/rebuild_registry/load_snapshot (dim1 evidence). No scripts preserved as standalone artifacts; documented narratively here and in README.md.
unknowns:
  - clean backend owner on 1.2.3 Windows: blocks_start (zero raw evidence; would require a fresh live-IDA win-native pass, e.g. searching disassembly around the known 1.0.9 pattern's neighbor functions, or command-name string-pool xref if the string still exists in the 1.2.3 binary -- not attempted this pass, no live IDA MCP access)
  - rebuild_registry / clean real UI trigger element: accepted as a genuine dim1 gap (not accepted_unknown -- red line 13 requires attempted breakthrough techniques, none attempted this pass for a frontend-extraction-tooling limitation, which is a different class of gap than an IDA decompile ceiling)
  - rebuild_registry full response DTO shape: blocks_start (not extracted this pass)
  - rebuild_registry call-tree beyond persist_registry to OS-syscall terminal: blocks_start (not traced this pass; 1.0.9 baseline traces this fully to file-write terminal, 1.2.3 does not yet)
  - RULE9 migration validity of any 1.0.9 evidence being treated as predictive of 1.2.3 clean behavior: explicitly NOT claimed -- 1.0.9 evidence is cited only as historical/product-behavior prior, not as migrated 1.2.3 evidence (no diff-confirmed-empty basis exists, since 1.2.3-side evidence for clean is entirely absent)
do_not_infer:
  - Do not infer that clean's 1.2.3 backend behaves identically to its 1.0.9 evidence (authBackupsRemoved/registryBackupsRemoved/staleEntriesRemoved fields) -- that is 1.0.9-specific evidence, cited for historical context only, not migrated per RULE9 (no diff-confirmed-empty basis).
  - Do not treat the "minified-static-function"/element:null frontend-control-flow.jsonl entries for clean/rebuild_registry as confirming a real UI call-site -- they only confirm the API wrapper function exists in the bundle.
  - Do not treat rebuild_registry as consumerStartReady on the strength of its dim2 evidence alone -- dim1 (uiCallEntryReady) is not satisfied, consistent with the stricter bar already applied to load_snapshot/logout in ../windows-1.2.3-accounts/.
  - Do not re-derive or duplicate load_snapshot's evidence in this package -- see ../windows-1.2.3-accounts/ for the authoritative, already-produced leaf.
  - Do not treat rebuild_registry_with_policy's extra policy parameter (vs 1.0.9's Repository::rebuild_registry, which took no such parameter per the 1.0.9 mac leaf.md) as understood -- the parameter's semantics (what policy values mean) are NOT derived this pass, only its existence is noted (see SYSTEM-DIFF.md).
