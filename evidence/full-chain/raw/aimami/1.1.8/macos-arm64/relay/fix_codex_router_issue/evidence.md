# Evidence — fix_codex_router_issue (AiMaMi 1.1.8, macos-arm64)

owner: `codexmate_lib::commands::relay::fix_codex_router_issue::he783bb26d80be6c4`
VA: `0x1004c5d10` | source_binary: `raw/binary/AiMaMi 1.1.8 universal.app/Contents/MacOS/AiMaMi`
session: `<审计会话>` | machine: `<本地机器>` | model: `claude-sonnet-5`
owner-map source: `intermediate/aimami/1.1.8/macos-arm64/version-delta/owner-map-118.jsonl` line matching `fix_codex_router_issue` (`source: func_query_clean_top_level_symbol`)
反编译器 live gate at start of this leaf: `status=ok, uptime_sec=213522.274, 反编译就绪=true, module=AiMaMi, idb_path=.../AiMaMi 1.1.8 universal.app/Contents/MacOS/AiMaMi.i64`

## Confirmed

1. Owner function fully decompiled at `0x1004c5d10` (8531 bytes real pseudocode, no truncation, no bail) — dispatches on the `itemId` arg: 3-byte `"all"` literal match routes to `fix_all` (0x1003f1dd0), anything else routes to `fix_issue(itemId)` (0x1003f5a80).
2. `is_process_running` (0x1001c0510) fully decompiled (2276 bytes): spawns `pgrep -x Codex` via `std::process::Command::new/arg/output` and returns true iff the subprocess reports success with output — same strategy as the 1.0.9 baseline, confirmed at the new VA independently this round.
3. `stop_codex_for_file_edit` (0x1001c0ce0) fully decompiled (1986 bytes): calls `request_codex_app_quit` (0x1001c0860) for a graceful quit, polls `is_process_running` in a 50ms `std::thread::functions::sleep` loop bounded by an `Instant`-based deadline built from a caller-supplied timeout (`std::time::Instant::now` + `Add<Duration>`), then unconditionally calls `kill_codex_processes_until_clear` (0x1001c2d50) as a force-kill fallback regardless of whether the graceful stop succeeded.
4. `write_atomic` (0x10039e600) fully decompiled (6678 bytes with header): confirmed crash-safe atomic-write pattern — `uuid::v4::Uuid::new_v4` for a unique temp-file suffix, `std::path::Path::_join`, `std::fs::OpenOptions::_open`, `std::io::Write::write_all`, `std::fs::File::sync_data` (fsync), `_close$NOCANCEL`, `std::sys::fs::rename` (the commit step), `std::sys::fs::remove_file` for stale-leftover cleanup, and `atomic_write::sync_parent_dir` for the parent-directory fsync. This is a genuine OS-syscall-boundary terminal, `terminated_reason=persistence_commit`, reached independently from 3 distinct call sites in this command's tree (`fix_config_stale_text`, `codex_writer::apply_codex_state`, `codex_catalog::write_catalog`).
5. `fix_auth_integrity` (0x1003dde60) fully decompiled (16514 bytes): makes a backup copy of an auth-related file via `std::fs::copy` before mutating it, and parses a JSON file via `serde_json::de::from_trait` + walks it via `serde_json::value::Value::pointer` for integrity validation — both real, independently-observed side effects.
6. `codex_catalog::remove_catalog` (0x100682470) callees confirm a direct `std::sys::fs::remove_file` (unlink) with no `write_atomic` indirection — a second, independently-confirmed `persistence_commit` terminal distinct from the write path.
7. Call-tree depth reaches **5** with two independent chains bottoming out at genuine OS-level terminals: `owner → fix_all/fix_issue → resync_codex_config → apply_codex_state/write_catalog → write_atomic → rename/fsync` (depth 5) and `owner → fix_all → resync_codex_config → remove_catalog → remove_file` (depth 4, additional terminal).
8. Literal Chinese status string decoded byte-for-byte from the owner's inline heap-write immediates (5 overlapping `mov` stores into a 37-byte allocation): `已自动重启 Codex 使修复生效` ("Automatically restarted Codex to apply the fix"), confirmed via manual little-endian byte reassembly of the exact immediate values seen in the decompiled code, cross-checked as valid UTF-8.
9. Two further literal string constants decoded the same way: 2-byte `"ok"` and 7-byte `"Success"`, both written into the final `Ok`-path response struct fields at `a1+8` and `a1+32`.
10. Frontend IPC contract confirms `argKeys: ["itemId"]` for `fix_codex_router_issue` in 1.1.8 (`raw/aimami/1.1.8/macos-arm64/frontend/ipc-contracts.jsonl` line 131), matching the 1.0.9 baseline's documented arg key exactly (no change).
11. `xrefs_to(0x1004c5d10)` returns a single call site (`0x10030ab75`, inside `codexmate_lib::run::{{closure}}::{{closure}}` at `0x10030a9e0`), confirming this is a live, dispatcher-wired Tauri command and not dead code.
12. 8 named per-issue fixer symbols are directly reachable from `fix_all`'s `callees()` result (`fix_config_stale_text`, `fix_auth_integrity`, `fix_config_third_party_text`, `fix_config_profile_conflict_text`, `fix_config_toml_syntax_text`, `resync_codex_config`, `fix_router_unlock_auth_residue`, `fix_takeover_backup_orphan`) — this reachability set is independent of the `fix_issue` body truncation (item below) since it was obtained via `callees()`, not `decompile()`.

## Inferred

1. Exact Rust field names for the `Ok`-path response struct at offsets `+8/+16` ("status"), `+24/+32/+40` ("message"), `+48/+56/+64` (unknown triple), `+144/+148` (count + bool flag) are plausible-shape guesses from offset/size patterns, not confirmed via `type_query`/`declare_type` against the 1.1.8 binary this round.
2. Mapping between 1.1.8's 8 named fixer symbols and 1.0.9's 7 byte-literal `itemId` match-arm keys is only partially confident: `fix_config_stale_text`↔`"config_stale"`, `fix_auth_integrity`↔`"auth_integrity"`, `fix_config_third_party_text`↔`"config_third_party"`, `fix_config_profile_conflict_text`↔`"config_profile_conflict"` are high-confidence name-pattern matches; `fix_config_toml_syntax_text`↔`"config_omit_syntax"`, `resync_codex_config`↔`"catalog_path_validity"`, `fix_router_unlock_auth_residue`↔`"db_orphan_providers"` are speculative semantic-overlap guesses only.
3. The new `codex_catalog::write_catalog`/`remove_catalog` provider-catalog JSON persistence path inside `resync_codex_config` is treated as a "new" observation this round, but since 1.0.9's `resync_codex_config` internals were themselves `accepted_unknown` in the 1.0.9 baseline leaf (never decompiled that round), this may be pre-existing 1.0.9 behavior only now disclosed by full decompilation, not a genuine 1.1.8 behavioral delta — Inferred, not asserted.
4. The `+48/+56/+64` and `+144/+148` fields of the `Ok`-path response struct are inferred to correspond to a fix-count summary and a "process was restarted" boolean flag, based on their position immediately following the process-restart logic in the owner body — not independently confirmed by cross-referencing frontend consumption code this round.
5. `checked_fix_result` (0x1003dd770), a callee of `fix_issue` per its refs list, is inferred to be a `Result`-wrapping helper based on its name only; its actual behavior was not decompiled this leaf.

## Unknown

[截断依赖节点已按 oversized 标注]
2. `fix_all`'s (0x1003f1dd0) full pseudocode body is likewise MCP-truncated at 1024 of 100797 total characters. A `basic_blocks(0x1003f5a80)` probe for `fix_issue` returned only 10 blocks / 286 bytes, which is inconsistent with the 232469-char decompiled-text size — this discrepancy was observed but not resolved this round (possibly a jump-table-driven dispatch that 反编译器's `basic_blocks` enumeration under-reports; not investigated further).
3. `kill_codex_processes_until_clear` (0x1001c2d50) body was not independently decompiled this leaf — only its call presence and demangled name are confirmed via `stop_codex_for_file_edit`'s refs list.
4. `checked_fix_result` (0x1003dd770) and `check_config_stale`'s (0x1003dba80) sub-callees `is_top_level_model_key`/`ok_item` bodies were not independently decompiled this leaf (only reachability via `callees()`).
5. `launch_codex_app` (0x1001bfa90) was fully decompiled (19570 bytes, not truncated) but not deeply analyzed for its exact `open -a`/bundle-identifier argument construction in this evidence pass — captured verbatim in `pseudocode/0010_launch_codex_app.c` for a future pass.
6. Windows-x64 equivalent of `fix_codex_router_issue` was not re-decompiled or diffed in this leaf (per `REVERSE-STATUS.md`, win partial coverage tracked separately: `diagnose_codex_router / fix_codex_router_issue / get_hotspot_enabled / has_notch / import_accounts_from_file / import_skill / parse_aimami_deeplink / reveal_relay_api_key / run_codex_router` remain win-partial as of this session).

## Method

反编译器 MCP (mac) `decompile`/`callees`/`xrefs_to` against the live 1.1.8 mac IDB (x86_64 slice of the universal Mach-O binary, arch-noise-filtered per `REVERSE-STATUS.md` precedent). Owner resolved via `intermediate/aimami/1.1.8/macos-arm64/version-delta/owner-map-118.jsonl`. 1.0.9 baseline cross-checked against `raw/aimami/1.0.9/macos/relay/fix_codex_router_issue/AI.md`. Frontend contract cross-checked against `raw/aimami/1.1.8/macos-arm64/frontend/ipc-contracts.jsonl` line 131. 反编译器 live gate passed once at session start (`健康检查`); no reconnects were needed during this leaf's tool calls.
