# interface — begin_chatgpt_oauth_login (AiMaMi 1.1.8, macos-arm64 label / x86_64 IDB slice)

## IPC contract (frontend-confirmed, `raw/aimami/1.1.8/macos-arm64/frontend/ipc-contracts.jsonl` line 13)

- **command**: `begin_chatgpt_oauth_login`
- **argKeys**: `[]` (no input parameters — void command)
- **frontend wrapper**: `beginChatgptOauthLogin: () => te("begin_chatgpt_oauth_login")` (`assets/index-CX-I_QAq.js:86`)
- **call site**: `accounts-page-*.js:80`, guarded by `if(a) return;` (an in-flight/lock guard — matches the
  backend Mutex-guarded singleton found at depth 1 of the call tree)
- **sibling command**: `cancel_chatgpt_oauth_login` (frontend-confirmed, not decompiled this leaf; shares
  the `OAUTH_CANCEL` static referenced in the owner's `refs[]`)
- **dispatch mechanism**: `tauri::ipc::InvokeResolver<R>::respond_async_serialized_inner::{{closure}}`
  (confirmed via `xrefs_to`) → async result is JSON-serialized back to the frontend
  (`terminated_reason=response_serialize`)

## Request DTO

None. Command takes no arguments; all inputs (PKCE verifier/challenge, state nonce) are generated
locally inside the command body.

## Response DTO (inferred from callee behavior, not decompiled at the field-struct level this round)

A `Result<String, Error>`-shaped async command returning the fully-rendered authorize URL string
(`build_authorize_url`'s return value, rendered via `<str as Display>::fmt`). The PKCE `code_verifier`
generated by `generate_pkce` is retained in local process state (not returned to the frontend) for later
use in the token-exchange step once the local callback listener receives the redirect.

## External HTTP/OAuth surface (string-pool confirmed via `find_regex`, not a decompiled call inside this
specific owner — the token-exchange POST is presumed to live in the callback-listener's accept-handler,
out of scope for this leaf; documented here for interface completeness)

- Authorize endpoint: `https://auth.openai.com/oauth/authorize`
- Token endpoint: `https://auth.openai.com/oauth/token` (`grant_type=authorization_code`,
  `code_verifier=...`)
- `User-Agent: AiMaMi/1.1.8`
- Public OAuth client id: `app_EMoamEEZ73f0CkXaXp7hrann` (not a confidential secret — public PKCE client
  identifier, same one used by OpenAI's own open-source Codex CLI login flow)
- `originator=codex_cli_rs`, `codex_cli_simplified_flow`, `id_token_add_organizations` — flow-selection
  flags matching the Codex CLI's own OAuth authorize parameters
- Local redirect/callback: `http://localhost:1455/auth/callback` (127.0.0.1:1455, bound by this command
  via `tokio::net::tcp::listener::TcpListener::bind_addr` → `mio::net::tcp::listener::TcpListener::bind`
  → libc `socket()`/`setsockopt(SO_REUSEADDR)`/`bind()`/`listen()`)

## Error paths

- `core::result::unwrap_failed` / `core::option::expect_failed` inside `build_authorize_url` on
  static-literal URL parse / query-mut failure — defensive-only, unreachable in normal operation (base
  URL and all static query keys are compile-time literals).
- `io::Error` propagated from `TcpListener::bind_addr` on bind failure (e.g. port 1455 already in use by
  another process) — propagates up through the owner closure's async body (not individually traced this
  round; the Err branch at `bind_addr+0x1b`/`+0x67` returns a tagged error value rather than panicking).

## Side effects

1. Binds a local TCP listener on `127.0.0.1:1455` for the duration of the OAuth flow (external network
   surface: listens on loopback only, not exposed beyond localhost).
2. Generates a PKCE code_verifier/code_challenge pair held in process memory (not persisted to disk by
   this command; persistence of the resulting tokens happens in a downstream command, out of scope here).
3. Acquires a `Mutex`-guarded global singleton (with adjacent `OAUTH_CANCEL` flag) so only one OAuth flow
   can be in progress at a time; matches the frontend's own `if(a) return;` re-entrancy guard.
4. Uses a `tokio::sync::oneshot` channel to hand off completion signaling to whatever accepts the
   callback connection (that accept-handler itself is not part of this owner and was not decompiled this
   round).
