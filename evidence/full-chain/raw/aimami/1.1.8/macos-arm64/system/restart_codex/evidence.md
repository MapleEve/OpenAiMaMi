# Evidence — `restart_codex` (AiMaMi 1.1.8, macos-arm64)

session `<审计会话>` | machine `<本地机器>` | model `claude-sonnet-5` | deep gold-leaf 20/29 (macos-arm64 batch) | 2026-07-13

## Owner-gate re-check before write

`find raw/aimami/1.1.8/macos-arm64/*/restart_codex/` returned empty prior to this write; `grep -c '"key":"aimami/1.1.8/macos-arm64/[^"]*restart_codex'` on root `INDEX.jsonl` prior to this write = 0; `grep -n "macos-arm64 restart_codex" internal-reverse/REVERSE-STATUS.md` prior to this write = 0 (only a v1.1.1 entry exists, line 367, a different version). Gate = ALLOW/first, no collision, no takeover token needed. `intermediate/aimami/1.1.8/macos-arm64/version-delta/owner-map-118.jsonl` line 90 already recorded `owner_va=0x1006c8fb0` (angle-A discovery pass only, no full leaf bundle) — this leaf supplies the first full raw pseudocode/call-tree/interface/evidence/gate-report package for 1.1.8.

## 分析工具就绪门

`健康检查` on mac 反编译器 confirmed `status=ok`, `uptime_sec=216767.711`, `反编译就绪=true`, `module="AiMaMi"`, `idb_path` containing `AiMaMi 1.1.8 universal.app`, `auto_analysis_ready=true`, `strings_cache_ready=true` (`strings_cache_size=14672`) — checked before any `decompile`/`callees`/`xrefs_to`/`get_string` call this round.

## Confirmed

1. Owner resolved via `intermediate/aimami/1.1.8/macos-arm64/version-delta/owner-map-118.jsonl` line 90: `codexmate_lib::commands::system::restart_codex::hb63e3eeac5d259e2` @ `0x1006c8fb0`, `source=func_query_clean_top_level_symbol`. Live `xrefs_to(0x1006c8fb0)` returned exactly 1 code xref, from `codexmate_lib::run::{{closure}}::{{closure}}@0x100303ca0` (call site `0x100303d07`) — confirms a live, dispatcher-wired IPC endpoint, not dead code.
2. Owner body fully decompiled (1921 raw chars, no truncation): calls `stop_codex_for_file_edit(5, 0)`, and only on its success discriminant (`10`) proceeds to call `launch_codex_app(3, 0)`; the final result is `Ok(())` only if both sub-calls return discriminant `10`, else `Err` via niche sentinel `0x8000000000000000` (a `CoreError::Display::fmt`-based conversion for the stop-failure path, with `unwrap_failed` as an internal Display-failure invariant guard, not a normally reachable branch).
3. `stop_codex_for_file_edit` (`0x1001c0ce0`, fully decompiled, 1986 chars): if `is_process_running()` is true, calls `request_codex_app_quit()` (error intentionally dropped, not propagated) then polls `is_process_running()` in an `Instant`/`Duration` 5-second-budget loop with 50ms `thread::sleep` between checks; unconditionally tail-calls `kill_codex_processes_until_clear()` regardless of poll outcome.
4. `request_codex_app_quit` (`0x1001c0860`, fully decompiled, 6748 chars): builds and spawns `std::process::Command::new("osascript").arg("-e").arg("tell application \"Codex\" to quit")` via `Command::output` — confirmed by `get_string` on the merged literal blob at `0x10125bfb4` (`"-b/Applications/Codex.apppgrep-xps-p-oosascript-etell application \"Codex\" to quitCodex.app/Codex Helper"`, 反编译器's contiguous-string-pool artifact containing all 6+ adjacent literals used across this call tree). This is a graceful, GUI-level AppleScript quit request targeting `Codex.app`/`Codex Helper`.
5. `kill_codex_processes_until_clear` (`0x1001c2d50`, fully decompiled, 12468 chars): calls `collect_codex_kill_targets()` to build a de-duplicated `HashMap<pid,name>`, then spawns a `kill` `Command` (literal `"kill"` confirmed adjacent to sqlite `-wal`/`-shm` literals at `0x10125b53c`, `get_string` value `"truetypetextuserkill-wal-shmlsof"`) against every collected target PID, looped with an `Instant`/`Duration` timeout + `sleep` re-check against `is_process_running()`.
6. `collect_codex_kill_targets` (`0x1001c0d90`, fully decompiled, 10445 chars) merges two independent PID sources: `list_codex_processes_matching()` (name-based, via `ps`) and `list_codex_db_holder_processes()` (lock-based, via `lsof` against the codex state-db path); each candidate is basename-filtered through `extract_process_name()` before being inserted into the target `HashMap`.
7. `list_codex_processes_matching` (`0x1001c13b0`, fully decompiled, 8291 chars): spawns `ps -A -o pid=,command=` via `Command::new`/`arg`/`arg`/`Command::output`; parses stdout via `String::from_utf8_lossy` + `trim_matches`.
8. `list_codex_db_holder_processes` (`0x1001c1c40`, fully decompiled, 29071 chars, no truncation, no `basic_blocks` chunking): resolves the local Codex home + state-db path (`CodexPaths::resolve_codex_home`/`resolve_codex_state_db`, local config/env lookup only, no network), spawns `lsof` against that path (plus its `-wal`/`-shm` companions), and explicitly excludes this process' own PID (`std::process::id`) from the resulting holder set.
9. `launch_codex_app` (`0x1001bfa90`, fully decompiled, 19570 raw chars, no truncation): spawns `open -b com.openai.Codex` (literal fragments `"-b"`/`"com.openai.Codex"` confirmed via refs + `get_string`), preceded by a `dirs::home_dir()` + `std::sys::fs::metadata()` existence probe on a candidate `/Applications/Codex.app` path, then polls `is_process_running()` for up to 3s (80ms sleep interval). On failure, allocates a fixed 22-byte error buffer — byte-count matches the 1.1.1 baseline's documented literal `"Codex launch timed out"` (22B) exactly; generic OS spawn/stat failures format `"open app failed: {io_error}"` (`aOpenAppFailed`).
10. `is_process_running` (`0x1001c0510`, fully decompiled, 2276 chars) is confirmed SHARED via 2 distinct callers (`stop_codex_for_file_edit`'s pre-quit gate + poll loop, and `launch_codex_app`'s post-relaunch poll loop) — both call sites verified via `refs`/`decompile` cross-check, not assumed.
11. `extract_process_name` (`0x1001c0680`, fully decompiled, 4419 chars): splits an input path/command string on `/` to extract the trailing basename component, used to compare candidate process names against the exclusion list (self/`AiMaMi.app`/`Cursor.app`/`.cursor/extensions` helpers — literal fragments confirmed via `get_string` at `0x10125c035`-`0x10125c069`: `"/.cursor/extensions//Cursor.app//AiMaMi.app//AiMaMi crashpad_handler-axpid=,command=-t-- | "` and its progressively-shorter overlapping substrings).
12. Frontend contract cross-checked: `argKeys=[]` confirmed via `raw/aimami/1.1.8/macos-arm64/frontend/ipc-contracts.jsonl` line 82 (`command="restart_codex"`, `kind="tauriInvokeWrapper"`); command listed as unchanged (`cmd-same.mac.txt` line 82) vs the 1.0.9 baseline.
13. Cross-version comparison against the existing 1.1.1 macOS full-chain leaf (`REVERSE-STATUS.md` line 367-401, session `<审计会话>`): identical 5-core-function set (`stop_codex_for_file_edit`/`launch_codex_app`/`request_codex_app_quit`/`kill_codex_processes_until_clear`/`is_process_running`), identical terminal syscall set (`pgrep`/`ps` process listing, `osascript` quit, `kill` forceful cleanup, `open` relaunch), identical `argKeys=[]`, identical response niche `0x8000000000000000`, and a byte-identical 22B `"Codex launch timed out"` error message — VA churn only (`0x100616150` -> `0x1006c8fb0`).

## Inferred

1. The exact `kill` invocation's signal argument (e.g., `-9`/SIGKILL vs a bare default `kill`) was not independently `get_bytes`-confirmed at the sub-byte-offset level this round; inferred as forceful/SIGKILL-equivalent from the function's naming (`kill_codex_processes_until_clear`) and its position after the graceful-quit path has already been attempted and its own timeout budget exhausted — consistent with the 1.1.1 baseline's documented `kill -9 pids` terminal-leaf label.
2. The exact `lsof` argument set (e.g., whether `-t` terse-PID-only mode is used, and whether the `-wal`/`-shm` companion paths are passed as 3 separate `lsof` invocations or one combined argument list) was not traced instruction-by-instruction; inferred from the co-located literal fragments (`"lsof"` adjacent to `"-wal-shm"` in the same constant blob) and the function's single `Command::output` call site.
3. The candidate fallback install path checked by `launch_codex_app`'s `dirs::home_dir()` + `metadata()` probe (beyond the confirmed literal `/Applications/Codex.app`) was not fully reconstructed; the `"DocumentsCodex"` fragment at `0x10125be63` suggests a secondary candidate path under the user's home directory (e.g. `~/Documents/...Codex...`), but the exact path-join logic (order of `/Applications/Codex.app` vs. the home-relative candidate) was not traced.
4. Whether `request_codex_app_quit`'s discarded error path (the `drop_in_place::<CoreError>` call when the quit command itself fails to spawn) is silently swallowed by design (best-effort graceful attempt) or is an oversight is inferred as intentional design, given the unconditional fallback to the forceful `kill_codex_processes_until_clear` path regardless of the graceful-quit outcome.
5. The precise semantics of `list_codex_processes_matching`'s process-name match predicate (exact string equality vs substring `is_contained_in` match against "Codex") is inferred from the `_$LT$$RF$str...Pattern$GT$::is_contained_in` symbol appearing in `collect_codex_kill_targets`'s ref list (used downstream of the raw `ps` output, likely for the final match decision after `extract_process_name` basename extraction), not independently step-traced instruction-by-instruction in this round.

## Unknown

1. Exact numeric constants for the `kill_codex_processes_until_clear` poll-loop timeout budget and sleep interval (byte offsets/values not individually `get_bytes`-extracted this round, unlike `stop_codex_for_file_edit`'s clearly-visible `5`/`50000000` and `launch_codex_app`'s clearly-visible `3`/`80000000`) — plain absence-of-extraction, not a decompile ceiling (the surrounding function decompiled cleanly with no truncation).
2. Exact struct layout / field offsets of the `HashMap<pid,name>` kill-target map built by `collect_codex_kill_targets` (hashbrown-based, standard library generic — not command-specific, out of leaf scope this round).
3. Windows-x64 equivalent full decompile/diff for `restart_codex` was not re-verified live this round (tracked separately per this session's round scope; a 1.1.1 Windows precedent set exists in `REVERSE-STATUS.md` but was not cross-checked against a live 1.1.8 win IDB in this specific sub-step).
4. The exact `pgrep`/`ps` output-column parsing logic beyond the confirmed `-o pid=,command=` format string (e.g., exact whitespace-splitting or column-index logic used to separate `pid` from `command` in the parsed stdout) was not traced instruction-by-instruction.
5. Whether any additional child/helper processes beyond the top-level `Codex`/`Codex Helper` pair (e.g., GPU/renderer helper processes, if the target app uses a multi-process architecture) are separately enumerated or covered incidentally by the generic `ps`-based name match — not confirmed either way this round; recorded as an open scope question, not asserted as covered or uncovered.

`recovery_attempts` note: all 5 Unknown items above are plain absence-of-extraction records (the underlying functions all decompiled cleanly, no truncation, no `basic_blocks` chunking bail, no async/vtable/ICF ceiling anywhere in this leaf) — none is claimed as `accepted_unknown`/`genuine_ceiling`, and none required a fake-wall taxonomy exclusion pass because no ceiling was ever hit.

## Fake-wall check

`accepted_unknown=false`; `genuine_ceiling=false`. All 10 functions in the call tree (owner + 9 callees) received a real Hex-Rays decompiled body; the largest (`list_codex_db_holder_processes`, 29071 raw chars) did not require `basic_blocks` chunking and showed no truncation marker. No `drop_in_place`-only stub was mistaken for a real body anywhere in this leaf; no async/poll state machine, no vtable/dynamic dispatch, no ICF folding encountered.

## renames / comments in IDB (this leaf)

None (owner already carries a real demangled Rust symbol `codexmate_lib::commands::system::restart_codex::hb63e3eeac5d259e2`, no `sub_xxxxx` rename needed). `append_comments` applied ×1 at the owner VA (`0x1006c8fb0`): "B-level owner for restart_codex; sha256=63c9655f7e7f0d725e55c83b5057023cb8e8351d577f47a9ad683137d6c89470; session <审计会话>; not gate promotion".
