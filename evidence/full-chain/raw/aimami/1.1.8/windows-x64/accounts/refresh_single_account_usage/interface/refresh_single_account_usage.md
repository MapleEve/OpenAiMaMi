# refresh_single_account_usage — windows-x64 1.1.8 interface (deep gold-leaf 13/20)

## Command identity
- name: `refresh_single_account_usage`, tracing-span literal embedded in owner: `("app", "refresh_single_account_usage", len 28)` (tracing target = `"app"`, matching the `namespace=app` convention already documented for sibling owners `has_notch`/`graceful_restart_for_update`).
- owner_va: `0x140a6c900` (renamed `refresh_single_account_usage_owner_sys`), size 4105B / 126 basic blocks / cyclomatic_complexity=32 — no separate thin dispatch wrapper; the owner IS the command's async `Future::poll` state machine body (4-case poll-state switch on the byte at `self+4736`).
- dispatcher/callers: `sub_1408E4D30`@`0x1408e4d30` and `sub_140900880`@`0x140900880` (both size `0x235`=565B) — a near-identical pair, consistent with the standard per-command async-fn poll-entry/drop-glue wrapper pattern seen elsewhere in this binary; not individually decompiled this leaf (generic dispatcher plumbing).
- module placement: `accounts/` — matches the existing `raw/aimami/1.1.8/windows-x64/accounts/begin_chatgpt_oauth_login/` precedent (that owner is independently confirmed to call the same shared `refresh_usage_response_build_sys`@`0x140352c90` helper as this command, grouping both under the account-management surface).

## Request DTO
- frontend `argKeys: ["accountKey"]` (confirmed via `raw/aimami/1.1.8/windows-x64/frontend/ipc-contracts.jsonl`, exact record: `{"command":"refresh_single_account_usage","argKeys":["accountKey"],"argObject":"{accountKey:e}","params":"e"}`) — single-string-argument command.
- backend: the literal `"accountKey"` (10 bytes, `aAccountkey_4`@`0x1413614f2`) is passed as the field descriptor into the shared, 43-caller `refresh_usage_arg_extract_poll_sys`@`0x14086fd70` poll helper; on `Ready(Ok)` the extracted bytes flow into the network-refresh path, on `Ready(Err)` a `Display`-formatted error string is built instead.

## Response DTO
- shape: `{"usage": <UsageData>}` — the literal `"usage"` (5 bytes, `aUsage_2`@`0x141361c7e`) is passed as the field-name argument into the shared `refresh_usage_response_build_sys`@`0x140352c90` helper (`sub_140352C90(v135+4352, &v89, aUsage_2, 5)`).
- `UsageData` fields: `usage_data_fields_flatten_sys`@`0x14037dc40` flattens ~13 `Option<T>`-niche-encoded fields (byte offsets 336/512/536/544/545/552/576/600/608/632/640/648/652 relative to the struct base) — consistent with a usage/quota-shaped record (byte-counters, a reset-timestamp-like i64, plan/tier flag bytes, and at least one `Option<Vec<u8>>`-shaped buffer resolved via `option_clone_if_present_helper_sys`@`0x14108f780`). Exact symbolic field names (e.g. `tokensUsed`/`quotaLimit`/`resetAt`) are Unknown — binary is fully stripped and no `serde` field-name string literals were found co-located with this struct's field offsets in this leaf's traced call-tree (see Unknown, evidence.md).
- niche/sentinel encoding: `0x8000000000000000` used as the `None`/absent sentinel for the `u64`-shaped optional fields, matching the standard Rust niche-optimization pattern used elsewhere in this binary (e.g. `has_notch`'s `Result<bool,_>` niche).

## Error paths
- accountKey-missing/invalid: handled entirely inside the shared `refresh_usage_arg_extract_poll_sys`/`poll_result_variant_predicate_sys` pair — terminates in generic `tracing::Span` record/close calls (`sub_14108F360`/`sub_14127E270`), not usage-specific business logic.
- `Display::fmt`-returned-an-error defensive guard: literal `"a Display implementation returned an error unexpectedly"` (`aADisplayImplem_11`@`0x141363370`), a generic Rust compiler-inserted guard for error-formatting code, appearing twice in this leaf's traced nodes (owner + arg-extract helper) — not `refresh_single_account_usage`-specific.
- network-refresh failure: the vtable-dispatched `dyn Future::poll` call inside `refresh_usage_poll_continuation_sys` can resolve to an `Err` variant that is niche-encoded and ultimately routed to `invoke_resolver_respond_sys` the same as the success path — the exact reqwest/hyper error-classification logic below the vtable dispatch was not separately re-traced this leaf (accepted `external_call` termination, see call-trees jsonl).

## Side effects
- network I/O: one outbound HTTP-shaped round trip via the vtable-dispatched `dyn Future::poll` call inside `refresh_usage_poll_continuation_sys`@`0x1400650e0` — this is the account-usage-refresh backend request (concrete transport internals not re-traced this leaf; consistent with the already-documented `get_relay_provider_quota` external_call termination pattern for this binary's reqwest/hyper/tokio stack).
- no file I/O, no registry access, no sidecar dispatch observed in any of the 8 traced nodes.
- generic runtime bookkeeping only otherwise: heap alloc/dealloc for the extracted `accountKey` bytes and the flattened `UsageData` buffers, atomic refcounting inside the shared `Arc`-drop helper (`sub_140473F20`, reached only as a stored drop-fn pointer, not directly call-graph-invoked from this leaf), and a `std::thread_local` Drop-destructor registration (`sub_14107F1B0`) for the boxed future state on the non-immediate-Ready branch.

## Delta vs 1.0.9 / 1.1.1 baselines
- **confirmed NEW in 1.1.8**: absent from the frontend command surface in both the 1.0.9 and 1.1.1 baselines checked this round — `intermediate/aimami/1.1.8/version-delta/frontend-delta/cmd-added.win.txt` and `cmd-added.mac.txt` both list `refresh_single_account_usage` as frontend-added; no `raw/aimami/1.1.1/windows-x64/*/refresh_single_account_usage/` directory and 0 occurrences in the 1.1.1 windows `frontend/ipc-contracts.jsonl`.
- **distinct sibling, not a rename**: 1.0.9 already shipped a related-but-different zero-argument command `refresh_usage_snapshot` (`raw/aimami/1.0.9/windows/system/refresh_usage_snapshot/`, owner `sub_1408CDB20`@`0x1408cdb20`, `strictImplementationUse`/`accepted_unknown: ICF/async-coroutine-decompile-failure`), which refreshes the **entire active-account list** and returns `CoreEnvelope<AccountListPayload>` with no arguments. `refresh_single_account_usage` is a genuinely new, narrower, single-account-targeted variant (`argKeys=["accountKey"]`, response shape `{"usage": UsageData}`) — both command strings (`"refresh_usage_snapshot"` and `"refresh_single_account_usage"`) co-exist in the same 1.1.8 owner's combined command-name string pool, confirming they are two distinct, still-coexisting commands in 1.1.8, not a rename/replacement of one by the other.
- unlike the 1.0.9 `refresh_usage_snapshot` owner (which hit a genuine `accepted_unknown` ceiling from async-coroutine ICF-fold complexity and required disasm-only recovery), this 1.1.8 `refresh_single_account_usage` owner decompiled **cleanly and completely** via plain `decompile()` with zero truncation across all 8 traced call-tree nodes.
