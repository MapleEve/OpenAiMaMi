# evidence — test_relay_provider_stream (AiMaMi 1.1.8, windows-x64)

session: <审计会话>
machine: <本地机器>
date: 2026-07-13
binary: AiMaMi 1.1.8 win64.exe (PE stripped x86_64)
sha256: f25d8129528b25ac7f69524fa07c78f961da189a7a8f2c758c288ec96bc68f10
imagebase: 0x140000000
idb_path: <证据库> 1.1.8 win64.exe.i64

## 分析工具就绪门

`健康检查` confirmed before any decompile/rename/set_comments call this round: `status=ok`, `uptime_sec=221588.662` (>>30s), `反编译就绪=true`, `module="AiMaMi 1.1.8 win64.exe"`, `idb_path=<证据库> 1.1.8 win64.exe.i64`, `auto_analysis_ready=true`, `strings_cache_ready=true`.

## Owner-gate re-check before write

`find raw/aimami/1.1.8/windows-x64/*/test_relay_provider_stream/` prior to this write returned empty; `grep -n '"key":"aimami/1.1.8/windows-x64/[^"]*test_relay_provider_stream'` on root `INDEX.jsonl` prior to this write = 0 hits (INDEX.jsonl line count 1632 at time of check) → gate=ALLOW/first, no collision, no takeover token needed. `intermediate/aimami/1.1.8/windows-x64/version-delta/owner-map-118.jsonl` already recorded `owner_va=0x140a2e5a0, present=true, method=find_regex_string_pool_hit+xrefs_to_owner=dedicated_per_command_wrapper_fn` (angle-A discovery pass only, no full leaf bundle) — this leaf supplies the first full raw pseudocode/call-tree/interface/evidence/gate-report package for this command in 1.1.8.

## Confirmed

- Owner VA `0x140A2E5A0`, renamed `test_relay_provider_stream_owner_sys`, size 8921B (0x22D9), 241 basic_blocks, cyclomatic_complexity=93, 2 code callers (`sub_1408D1850`/`sub_1409026F0`, both size 0x235=565B) — both renamed this round to `test_relay_provider_stream_async_spawn_a_sys`/`_b_sys`, matching the 1.1.1 windows baseline's byte-identical 565B async-wrapper pattern (`0x140A81E90`/`0x140AB1E40`). Batch rename this round: `total=3, ok=3, failed=0`.
- Full owner body decompiled with **0 truncation** this round via `decompile(addr=0x140a2e5a0, include_addresses=false)` → 44982 characters, 64 distinct instruction-level refs. This is a materially deeper capture than either the 1.1.1 baseline (`owner.md` documents structure/callers/vtable but not a full linear decompile) or this session's own `test_relay_draft_stream` sibling (which relied on `analyze_function` summaries plus one full decompile of the smaller 4637B owner).
- Frontend argKeys `["providerId","model","onEvent"]` confirmed at `raw/aimami/1.1.8/windows-x64/frontend/ipc-contracts.jsonl` line 127, cross-validated byte-exact against 4 owner-body literal refs: `manager`(7B,0x1413613b0, Tauri State<T> injection), `providerId`(10B,0x141361574), `model`(5B,0x14136157e), `onEvent`(7B,0x141361583).
- providerId→provider lookup mechanism fully traced: Windows atomic lock acquire (`sub_1412AC970`/`sub_1412ABCB0`) → linear Vec scan with entry-match predicate `sub_141276960` (199B, 0-callee leaf) → matched-entry Option-field clone via `option_clone_if_present_helper_sys`@0x14108F780 (6 call sites) → capturedAt-style timestamp write via `sub_14027CA20` → quota-check via `sub_14027A5F0` (660B, own nested lock+`WakeByAddressSingle`+poison-panic, string-confirmed `src\core\relay\manager.rs`).
- 3 distinct conditional panic/error sites confirmed, all funneling to shared `sub_1412AD780`: providers-Vec lock poison (`"relay state poisoned"`, 20B@0x141362450, first seen this round, absent from the draft sibling), quota-check's own internal lock poison (2nd independent call site, same string), and the generic Display-impl format-error check via `sub_140E156C0` (`"a Display implementation returned an error unexpectedly"`, 55B@0x141363370, byte-identical shared call shape with the draft sibling).
- Response envelope builder `sub_140035530` (538B, 23 basic_blocks, cc=13) directly caller-list-confirmed (`xrefs_to`) to have exactly 2 callers: `test_relay_draft_stream_owner_sys` and this owner — proving the `{schemaVersion,success,code,message}` envelope shape is shared verbatim between both `test_relay_*_stream` sibling commands.
- Shared SSE executor `sub_1409717F0` (12059B, 58 direct callees) confirmed as an owner-direct callee via the full decompile, at the SAME address already used identically by `test_relay_draft_stream_owner_sys` this session — strong structural proof both stream-test commands funnel into one shared HTTP/SSE engine, reaching the same established `relay_http_request_cleanup_sys` terminus (19-caller shared cleanup, `terminated_reason=sidecar_dispatch`).
- Final IPC dispatch via `invoke_resolver_respond_sys` (shared, already established this session in the `refresh_single_account_usage` leaf) confirmed as the unconditional final call on every owner exit path.
- Command confirmed absent from the 1.0.9 baseline: `intermediate/aimami/1.1.8/windows-x64/version-delta/win-divergence-118.json` lists `test_relay_provider_stream` under `added_win_1_1_8` (relative to 1.0.9), and `intermediate/aimami/1.1.8/version-delta/frontend-delta/cmd-added.win.txt` line 14 lists it as frontend-added; the same divergence file lists `test_relay_provider` (non-streaming, 1.0.9-era name) under `removed_win_1_1_8`, and a pre-existing 1.0.9 frontend CCF cross-reference documented in the 1.1.1 baseline's `interface.md` records `test_relay_provider argKeys=[manager,providerId]` (non-stream) — confirming this streaming command is a **rename+redesign of the 1.0.9 `test_relay_provider` command**, not a bolt-on addition with zero prior history. A 1.1.1 windows-x64 baseline leaf already exists at `raw/aimami/1.1.1/windows-x64/commands_relay/test_relay_provider_stream/` (owner then at `0x1409C98D0`, 7689B) — so relative to **1.1.1** this command is pre-existing with VA churn only (owner grew 7689B→8921B, +1232B/+16%), not newly introduced.

## Inferred

- The `RelayProvider` Vec scan stride is consistent with the 232B/entry stride already documented elsewhere this session for the relay-providers Vec (`reorder_relay_providers`/`get_relay_provider_quota` sub-entries), though the exact stride was not independently re-measured byte-for-byte on this owner's specific Vec-index structure this round (the owner's index-entry stride observed in the decompiled loop is 32B per index record, referencing the larger 232B provider records indirectly — the two strides are not contradictory, consistent with an index-of-pointers-into-records layout, but this relationship is inferred from call shape rather than independently confirmed via a struct dump).
- The quota-check discriminant `10` returned by `sub_14027A5F0` most likely corresponds to "quota OK / proceed to stream" (consumed at owner line ~617 to gate the `sub_1409717F0` SSE call), by analogy with the 1.1.1 baseline's documented error-code table (`0x8000000000000000` = quota miss) — the exact mapping of every non-10 discriminant value was not independently re-derived this round.
- The `providerId`/`model` pair forwarded into `sub_1409717F0` likely selects among the same ~11-provider routing table already confirmed in this session's `get_relay_provider_quota` leaf (kimi.com/siliconflow/openrouter/novita/deepseek/stepfun/moonshot/MiniMax host set) given the shared executor address, but this was not independently re-confirmed via a fresh `find_regex` sweep scoped to this exact command's code path this round.

## Unknown

- **Post-stream `relay.json` persistence continuity vs 1.1.1**: the 1.1.1 baseline's `interface.md` explicitly documents a **unique side effect** of this command — after the stream completes, `relay_providers_config_write_and_persist_sys`@0x1406E6960 (1.1.1 VA) atomically writes `relay.json` to disk, updating the matched provider's quality score based on stream latency (<200ms→100, 200-599ms→70, 600-1499ms→40, >=1500ms→20). This round: (a) this exact function name/VA is **not** among this owner's 64 confirmed direct refs/callees; (b) `REVERSE-STATUS.md` records this same symbol name at the same VA `0x1406E6960` as already-renamed and shared with `reorder_relay_providers`/`set_relay_display_tags` earlier this session, but a **live re-check this round** (`analyze_function(addr=0x1406E6960)`) returned `name=sub_1406E6920` (unrenamed, function actually starts 0x40 earlier) with `callers=[]` — contradicting the narrative claim; `xrefs_to(addr=0x1406E6960)` also returned 0 xrefs live. This is documented as a plain Unknown/possible cross-session note-staleness, **not silently trusted** from the prior narrative (per redline 14 GROUNDED_NOT_SUMMARY). `recovery_attempts`: live `analyze_function`+`xrefs_to` both run this round on the documented VA; neither corroborates a direct link from this owner's call graph to that address. If the side effect exists in 1.1.8, it is most likely reached from deep inside the un-decompiled `sub_1409717F0` (12KB Hex-Rays render-limit wall) after the stream completes, not from a top-level owner callee — this was not independently traced this round given the same wall already hit by the draft sibling.
- Exact `RelayProvider` struct field names/offsets for the 6 Option-cloned config fields (baseUrl/apiKey-shaped) — offsets known (52+72/96/120/152/176/200/224/248/272/296/320) but not mapped to symbolic field names via a `type_query`/struct-layout pass this round.
- Whether the 1.1.1-documented `warnings`/`data` response-envelope fields are still present on the 1.1.8 `sub_140035530` envelope — only `schemaVersion`/`success`/`code`/`message` were independently re-confirmed via direct instruction-level refs this round; `warnings`/`data` were not found in this function's own ref list (though present in the wider, noisier merged string-pool blob near this address, which is not reliable per-function attribution given known Rust LTO string-pool merging).
- `sub_1409717F0`'s full request-construction body (auth header selection, exact per-provider endpoint URL construction, timeout/retry config) — known Hex-Rays render-limit wall (`decompile()` truncates at 1023 chars regardless of `include_addresses`), same wall already independently hit for this exact address by the draft sibling this session; `genuine_ceiling` is **not** claimed (architecture_only/budget-rule taxonomy per redline 13, not a fake wall) — listed as an open item for `readyToImplement`, not silently skipped.
- Exact semantic distinction between the 3 conditional panic sites' downstream consumer-facing behavior (whether all 3 surface as the same generic `code`/`message` shape to the frontend, or differ) — not independently traced.

## Fake-wall check (redline 13)

`genuine_ceiling=false` for this owner; `accepted_unknown` is **not** claimed anywhere in this leaf. The one genuine architecture-only wall encountered (`sub_1409717F0`'s 12KB body) was independently re-attempted this round (`decompile` with both `include_addresses=true` and `false`, both truncate at ~1023 chars) before being classified as a known Hex-Rays render-limit matching the exact same wall already documented for this address by the draft sibling this round — not a fresh unverified claim.

## dim6 / gate tier

`gate_tier=strictImplementationUse_candidate`. `implementation_use=false`, `gate_accepted=false` — this leaf is evidence collection only, **not gate promotion**, per redline 2/11 and the SOP task framing ("not gate promotion").
