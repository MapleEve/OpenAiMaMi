# evidence — codexmate_lib4core12debug_bundle13catalog_shape (mac 1.2.2) NEW delta

## Confirmed
- owner 0x1003ef710; call-tree depth=2 节点=20(真实decompile, 截断守卫)。
- 关键字符串: `addr`, `allRouterModelsrelayModelsstatus`, `code`, `codex_router_catalog.jsonmodels_cache.json`, `compatible`, `extraInRouter`, `fieldComparison`, `mismatch[event][previousarchivedidentitytrailers`, `missingFromRouter`, `modelsCache`, `models_cache.json`, `pathExists`
- 副作用: `serde_json`

## Inferred
- 新命令行为。
- 

## Delta
- 1.1.8→1.2.2 新增命令。
